<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>C++ STL常用容器API总结</title>
    <url>/posts/ce863392/</url>
    <content><![CDATA[<h2 id="C-STL"><a href="#C-STL" class="headerlink" title="C++ STL"></a>C++ STL</h2><p>C++ STL（标准模板库）是一套功能强大的 C++ 模板类，提供了通用的模板类和函数，这些模板类和函数可以实现多种流行和常用的算法和数据结构，如向量、链表、队列、栈。</p>
<p>C++ 标准模板库的核心包括以下三个组件：</p>
<ul>
<li><strong>容器（Containers）</strong>: 容器是用来管理某一类对象的集合。C++ 提供了各种不同类型的容器，比如 deque、list、vector、map 等。</li>
<li><strong>算法（Algorithms）</strong>: 算法作用于容器。它们提供了执行各种操作的方式，包括对容器内容执行初始化、排序、搜索和转换等操作。</li>
<li><strong>迭代器（iterators）</strong>: 迭代器用于遍历对象集合的元素。这些集合可能是容器，也可能是容器的子集。</li>
</ul>
<p>这三个组件都带有丰富的预定义函数，帮助我们通过简单的方式处理复杂的任务。</p>
<span id="more"></span>

<h2 id="C-STL常用容器"><a href="#C-STL常用容器" class="headerlink" title="C++ STL常用容器"></a>C++ STL常用容器</h2><ul>
<li><code>vector</code>：向量</li>
<li><code>stack</code>：栈</li>
<li><code>set</code>：集合</li>
<li><code>queue</code>：队列</li>
<li><code>list</code>：双向链表</li>
<li><code>map</code>：地图</li>
<li><code>string</code>：字符串</li>
<li><code>deque</code>：双端队列</li>
<li><code>priority_queue</code>：优先队列</li>
</ul>
<p>对于容器，主要的操作有：</p>
<ul>
<li>容器的建立</li>
<li>插入元素</li>
<li>删除元素</li>
<li>查询</li>
<li>遍历</li>
<li>计算元素个数</li>
<li>检查元素是否为空</li>
<li>输出容器包含的内容</li>
</ul>
<h2 id="vector"><a href="#vector" class="headerlink" title="vector"></a>vector</h2><p><code>vector</code>包含着一系列连续存储的元素,其行为和数组类似。访问Vector中的任意元素或从末尾添加元素都可以在<code>常量级时间复杂度</code>内完成，而查找特定值的元素所处的位置或是在Vector中插入元素则是<code>线性时间复杂度</code>。</p>
<p>构造函数:</p>
<ul>
<li><p><code>vector();</code> 无参数 - 构造一个空的vector</p>
</li>
<li><p><code>vector(size_type num);</code> 数量(num) - 构造一个大小为num，值为Type默认值的Vector</p>
</li>
<li><p><code>vector(size_type num, const TYPE &amp;val);</code> 数量(num)和值(val) - 构造一个初始放入num个值为val的元素的Vector</p>
</li>
<li><p><code>vector(const vector &amp;from);</code> vector(from) - 构造一个与vector from 相同的vector</p>
</li>
<li><p><code>vector(input_iterator start, input_iterator end);</code> 迭代器(start)和迭代器(end) - 构造一个初始值为[start,end)区间元素的Vector(注:半开区间).</p>
</li>
<li><pre><code>vector(initializer_list&lt;value_type&gt; il, const allocator_type&amp; alloc = allocator_type());
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">   </span><br><span class="line"></span><br><span class="line">  C++11新提供的方法，类似如下方式：</span><br><span class="line"></span><br><span class="line">  - `std::vector&lt;int&gt;a&#123;1, 2, 3, 4, 5&#125;;`</span><br><span class="line">  - `std::vector&lt;int&gt;a = &#123;1, 2, 3, 4, 5&#125;;`</span><br><span class="line"></span><br><span class="line">常用API：</span><br><span class="line"></span><br><span class="line">- Operators : 对vector进行赋值或比较</span><br><span class="line">  - `v1 == v2`</span><br><span class="line">  - `v1 != v2`</span><br><span class="line">  - `v1 &lt;= v2`</span><br><span class="line">  - `v1 &gt;= v2`</span><br><span class="line">  - `v1 &lt; v2`</span><br><span class="line">  - `v1 &gt; v2`</span><br><span class="line">  - `v[]`</span><br><span class="line">- `assign()`对Vector中的元素赋值</span><br><span class="line">- `at()` : 返回指定位置的元素</span><br><span class="line">- `back()` : 返回最末一个元素</span><br><span class="line">- `begin()` : 返回第一个元素的迭代器</span><br><span class="line">- `capacity()` : 返回vector所能容纳的元素数量(在不重新分配内存的情况下）</span><br><span class="line">- `clear()` : 清空所有元素</span><br><span class="line">- `empty()` : 判断Vector是否为空（返回true时为空）</span><br><span class="line">- `end()` : 返回最末元素的迭代器(译注:实指向最末元素的下一个位置)</span><br><span class="line">- `erase()` : 删除指定元素</span><br><span class="line">- `front()` : 返回第一个元素</span><br><span class="line">- `get_allocator()` : 返回vector的内存分配器</span><br><span class="line">- `insert()` : 插入元素到Vector中</span><br><span class="line">- `max_size()` : 返回Vector所能容纳元素的最大数量（上限）</span><br><span class="line">- `pop_back()` : 移除最后一个元素</span><br><span class="line">- `push_back()` : 在Vector最后添加一个元素</span><br><span class="line">- `rbegin()` : 返回Vector尾部的逆迭代器</span><br><span class="line">- `rend()` : 返回Vector起始的逆迭代器</span><br><span class="line">- `reserve()` : 设置Vector最小的元素容纳数量</span><br><span class="line">- `resize()` : 改变Vector元素数量的大小</span><br><span class="line">- `size()` : 返回Vector元素数量的大小</span><br><span class="line">- `swap()` : 交换两个Vector</span><br><span class="line"></span><br><span class="line">## stack</span><br><span class="line"></span><br><span class="line">C++ stack（堆栈）实现了一个**先进后出**（FILO）的数据结构。</span><br><span class="line"></span><br><span class="line">构造函数：</span><br><span class="line"></span><br><span class="line">- `stack&lt;T&gt; stkT;` : 采用模板类实现，stack对象的默认构造形式</span><br><span class="line">- `stack(const stack &amp;stk);` : 拷贝构造函数</span><br><span class="line"></span><br><span class="line">常用方法：</span><br><span class="line"></span><br><span class="line">- `size()`: 返回栈中的元素数</span><br><span class="line">- `top()`: 返回栈顶的元素</span><br><span class="line">- `pop()`: 从栈中取出并删除元素</span><br><span class="line">- `push(x)`: 向栈中添加元素x</span><br><span class="line">- `empty()`: 在栈为空时返回true</span><br><span class="line"></span><br><span class="line">## set</span><br><span class="line"></span><br><span class="line">Sets are containers that store unique elements following a specific order.</span><br><span class="line"></span><br><span class="line">集合中以一种特定的顺序保存唯一的元素。</span><br><span class="line"></span><br><span class="line">构造函数:</span><br><span class="line"></span><br><span class="line">- `set();` 无参数 - 构造一个空的set</span><br><span class="line"></span><br><span class="line">- `set(InputIterator first, InputIterator last)` : 迭代器的方式构造set</span><br><span class="line"></span><br><span class="line">- `set(const set &amp;from);` : copyd的方式构造一个与set from 相同的set</span><br><span class="line"></span><br><span class="line">- `set(input_iterator start, input_iterator end);` 迭代器(start)和迭代器(end) - 构造一个初始值为[start,end)区间元素的Vector(注:半开区间).</span><br><span class="line"></span><br><span class="line">- ```</span><br><span class="line">  set (initializer_list&lt;value_type&gt; il, const key_compare&amp; comp = key_compare(), const allocator_type&amp; alloc = allocator_type());</span><br></pre></td></tr></table></figure>

 

C++11新提供的方法，类似如下方式：

- `std::set&lt;int&gt;a&#123;1, 2, 3, 4, 5&#125;;`
</code></pre>
</li>
</ul>
<p>example:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// constructing sets</span><br><span class="line">#include &lt;iostream&gt;</span><br><span class="line">#include &lt;set&gt;</span><br><span class="line"></span><br><span class="line">bool fncomp (int lhs, int rhs) &#123;return lhs&lt;rhs;&#125;</span><br><span class="line"></span><br><span class="line">struct classcomp &#123;</span><br><span class="line">  bool operator() (const int&amp; lhs, const int&amp; rhs) const</span><br><span class="line">  &#123;return lhs&lt;rhs;&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">int main ()</span><br><span class="line">&#123;</span><br><span class="line">  std::set&lt;int&gt; first;                           // empty set of ints</span><br><span class="line"></span><br><span class="line">  int myints[]= &#123;10,20,30,40,50&#125;;</span><br><span class="line">  std::set&lt;int&gt; second (myints,myints+5);        // range</span><br><span class="line"></span><br><span class="line">  std::set&lt;int&gt; third (second);                  // a copy of second</span><br><span class="line"></span><br><span class="line">  std::set&lt;int&gt; fourth (second.begin(), second.end());  // iterator ctor.</span><br><span class="line"></span><br><span class="line">  std::set&lt;int,classcomp&gt; fifth;                 // class as Compare</span><br><span class="line"></span><br><span class="line">  bool(*fn_pt)(int,int) = fncomp;</span><br><span class="line">  std::set&lt;int,bool(*)(int,int)&gt; sixth (fn_pt);  // function pointer as Compare</span><br><span class="line"></span><br><span class="line">  return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>常用API：</p>
<ul>
<li><code>begin()</code> : 返回指向第一个元素的迭代器</li>
<li><code>clear()</code> : 清除所有元素</li>
<li><code>count()</code> : 返回某个值元素的个数</li>
<li><code>empty()</code> : 如果集合为空，返回true</li>
<li><code>end()</code> : 返回指向最后一个元素的迭代器</li>
<li><code>equal_range()</code> : 返回集合中与给定值相等的上下限的两个迭代器</li>
<li><code>erase()</code> : 删除集合中的元素</li>
<li><code>find()</code> : 返回一个指向被查找到元素的迭代器</li>
<li><code>get_allocator()</code> : 返回集合的分配器</li>
<li><code>insert()</code> : 在集合中插入元素</li>
<li><code>lower_bound()</code> : 返回指向大于（或等于）某值的第一个元素的迭代器</li>
<li><code>key_comp()</code> : 返回一个用于元素间值比较的函数</li>
<li><code>max_size()</code> : 返回集合能容纳的元素的最大限值</li>
<li><code>rbegin()</code> : 返回指向集合中最后一个元素的反向迭代器</li>
<li><code>rend()</code> : 返回指向集合中第一个元素的反向迭代器</li>
<li><code>size()</code> : 集合中元素的数目</li>
<li><code>swap()</code> : 交换两个集合变量</li>
<li><code>upper_bound()</code> : 返回大于某个值元素的迭代器</li>
<li><code>value_comp()</code> : 返回一个用于比较元素间的值的函数</li>
</ul>
<h2 id="queue"><a href="#queue" class="headerlink" title="queue"></a>queue</h2><p>C++队列是一种容器适配器，它给予程序员一种先进先出(FIFO)的数据结构。</p>
<p>构造函数：</p>
<ul>
<li><code>explicit queue (const container_type&amp; ctnr);</code></li>
<li><code>explicit queue (container_type&amp;&amp; ctnr = container_type());</code></li>
<li><code>template &lt;class Alloc&gt; explicit queue (const Alloc&amp; alloc);</code></li>
<li><code>template &lt;class Alloc&gt; queue (const container_type&amp; ctnr, const Alloc&amp; alloc);</code></li>
<li><code>template &lt;class Alloc&gt; queue (container_type&amp;&amp; ctnr, const Alloc&amp; alloc);</code></li>
<li><code>template &lt;class Alloc&gt; queue (const queue&amp; x, const Alloc&amp; alloc);</code></li>
<li><code>template &lt;class Alloc&gt; queue (queue&amp;&amp; x, const Alloc&amp; alloc);</code></li>
</ul>
<p>example:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// constructing queues</span><br><span class="line">#include &lt;iostream&gt;       // std::cout</span><br><span class="line">#include &lt;deque&gt;          // std::deque</span><br><span class="line">#include &lt;list&gt;           // std::list</span><br><span class="line">#include &lt;queue&gt;          // std::queue</span><br><span class="line"></span><br><span class="line">int main ()</span><br><span class="line">&#123;</span><br><span class="line">  std::deque&lt;int&gt; mydeck (3,100);        // deque with 3 elements</span><br><span class="line">  std::list&lt;int&gt; mylist (2,200);         // list with 2 elements</span><br><span class="line"></span><br><span class="line">  std::queue&lt;int&gt; first;                 // empty queue</span><br><span class="line">  std::queue&lt;int&gt; second (mydeck);       // queue initialized to copy of deque</span><br><span class="line"></span><br><span class="line">  std::queue&lt;int,std::list&lt;int&gt;&gt; third; // empty queue with list as underlying container</span><br><span class="line">  std::queue&lt;int,std::list&lt;int&gt;&gt; fourth (mylist);</span><br><span class="line"></span><br><span class="line">  std::cout &lt;&lt; &quot;size of first: &quot; &lt;&lt; first.size() &lt;&lt; &#x27;\n&#x27;;</span><br><span class="line">  std::cout &lt;&lt; &quot;size of second: &quot; &lt;&lt; second.size() &lt;&lt; &#x27;\n&#x27;;</span><br><span class="line">  std::cout &lt;&lt; &quot;size of third: &quot; &lt;&lt; third.size() &lt;&lt; &#x27;\n&#x27;;</span><br><span class="line">  std::cout &lt;&lt; &quot;size of fourth: &quot; &lt;&lt; fourth.size() &lt;&lt; &#x27;\n&#x27;;</span><br><span class="line"></span><br><span class="line">  return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Output:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">size of first: 0</span><br><span class="line">size of second: 3</span><br><span class="line">size of third: 0</span><br><span class="line">size of fourth: 2</span><br></pre></td></tr></table></figure>

<p>常用API：</p>
<ul>
<li><code>back()</code> : 返回最后一个元素</li>
<li><code>empty()</code> : 如果队列空则返回真</li>
<li><code>front()</code> : 返回第一个元素</li>
<li><code>pop()</code> : 删除第一个元素</li>
<li><code>push()</code> : 在末尾加入一个元素</li>
<li><code>size()</code> : 返回队列中元素的个数</li>
</ul>
<h2 id="list"><a href="#list" class="headerlink" title="list"></a>list</h2><p>Lists将元素按顺序储存在链表中. 与 向量(vectors)相比, 它允许快速的插入和删除，但是随机访问却比较慢.</p>
<p>构造函数：</p>
<ul>
<li><code>list (const allocator_type&amp; alloc = allocator_type());</code></li>
<li><code>list (size_type n, const value_type&amp; val = value_type(), const allocator_type&amp; alloc = allocator_type());</code></li>
<li><code>template &lt;class InputIterator&gt; list (InputIterator first, InputIterator last, const allocator_type&amp; alloc = allocator_type());</code></li>
<li><code>list (const list&amp; x);</code></li>
</ul>
<p>常用API：</p>
<ul>
<li><code>assign()</code> : 给list赋值</li>
<li><code>back()</code> : 返回最后一个元素</li>
<li><code>begin()</code> : 返回指向第一个元素的迭代器</li>
<li><code>clear()</code> : 删除所有元素</li>
<li><code>empty()</code> : 如果list是空的则返回true</li>
<li><code>end()</code> : 返回末尾的迭代器</li>
<li><code>erase()</code> : 删除一个元素</li>
<li><code>front()</code> : 返回第一个元素</li>
<li><code>get_allocator()</code> : 返回list的配置器</li>
<li><code>insert()</code> : 插入一个元素到list中</li>
<li><code>max_size()</code> : 返回list能容纳的最大元素数量</li>
<li><code>merge()</code> : 合并两个list</li>
<li><code>pop_back()</code> : 删除最后一个元素</li>
<li><code>pop_front()</code> : 删除第一个元素</li>
<li><code>push_back()</code> : 在list的末尾添加一个元素</li>
<li><code>push_front()</code> : 在list的头部添加一个元素</li>
<li><code>rbegin()</code> : 返回指向第一个元素的逆向迭代器</li>
<li><code>remove()</code> : 从list删除元素</li>
<li><code>remove_if()</code> : 按指定条件删除元素</li>
<li><code>rend()</code> : 指向list末尾的逆向迭代器</li>
<li><code>resize()</code> : 改变list的大小</li>
<li><code>reverse()</code> : 把list的元素倒转</li>
<li><code>size()</code> : 返回list中的元素个数</li>
<li><code>sort()</code> : 给list排序</li>
<li><code>splice()</code> : 合并两个list</li>
<li><code>swap()</code> : 交换两个list</li>
<li><code>unique()</code> : 删除list中重复的元素</li>
</ul>
<h2 id="map"><a href="#map" class="headerlink" title="map"></a>map</h2><p>C++ Maps是一种关联式容器，包含“关键字&#x2F;值”对</p>
<p>构造函数：</p>
<ul>
<li><code>map (const key_compare&amp; comp = key_compare(), const allocator_type&amp; alloc = allocator_type());</code></li>
<li><code>template &lt;class InputIterator&gt; map (InputIterator first, InputIterator last,const key_compare&amp; comp = key_compare(),const allocator_type&amp; = allocator_type());</code></li>
<li><code>map (const map&amp; x);</code></li>
<li><code>map (const map&amp; x, const allocator_type&amp; alloc);</code></li>
<li><code>map (map&amp;&amp; x);</code></li>
<li><code>map (map&amp;&amp; x, const allocator_type&amp; alloc);</code></li>
<li><code>map (initializer_list&lt;value_type&gt; il, const key_compare&amp; comp = key_compare(), const allocator_type&amp; alloc = allocator_type());</code></li>
</ul>
<p>example:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// constructing maps</span><br><span class="line">#include &lt;iostream&gt;</span><br><span class="line">#include &lt;map&gt;</span><br><span class="line"></span><br><span class="line">bool fncomp (char lhs, char rhs) &#123;return lhs&lt;rhs;&#125;</span><br><span class="line"></span><br><span class="line">struct classcomp &#123;</span><br><span class="line">  bool operator() (const char&amp; lhs, const char&amp; rhs) const</span><br><span class="line">  &#123;return lhs&lt;rhs;&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">int main ()</span><br><span class="line">&#123;</span><br><span class="line">  std::map&lt;char,int&gt; first;</span><br><span class="line"></span><br><span class="line">  first[&#x27;a&#x27;]=10;</span><br><span class="line">  first[&#x27;b&#x27;]=30;</span><br><span class="line">  first[&#x27;c&#x27;]=50;</span><br><span class="line">  first[&#x27;d&#x27;]=70;</span><br><span class="line"></span><br><span class="line">  std::map&lt;char,int&gt; second (first.begin(),first.end());</span><br><span class="line"></span><br><span class="line">  std::map&lt;char,int&gt; third (second);</span><br><span class="line"></span><br><span class="line">  std::map&lt;char,int,classcomp&gt; fourth;                 // class as Compare</span><br><span class="line"></span><br><span class="line">  bool(*fn_pt)(char,char) = fncomp;</span><br><span class="line">  std::map&lt;char,int,bool(*)(char,char)&gt; fifth (fn_pt); // function pointer as Compare</span><br><span class="line"></span><br><span class="line">  return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>常用API：</p>
<ul>
<li><code>begin()</code> : 返回指向map头部的迭代器</li>
<li><code>clear()</code> : 删除所有元素</li>
<li><code>count()</code> : 返回指定元素出现的次数</li>
<li><code>empty()</code> : 如果map为空则返回true</li>
<li><code>end()</code> : 返回指向map末尾的迭代器</li>
<li><code>equal_range()</code> : 返回特殊条目的迭代器对</li>
<li><code>erase()</code> : 删除一个元素</li>
<li><code>find()</code> : 查找一个元素</li>
<li><code>get_allocator()</code> : 返回map的配置器</li>
<li><code>insert()</code> : 插入元素–</li>
<li><code>key_comp()</code> : 返回比较元素key的函数</li>
<li><code>lower_bound()</code> : 返回键值&gt;&#x3D;给定元素的第一个位置</li>
<li><code>max_size()</code> : 返回可以容纳的最大元素个数</li>
<li><code>rbegin()</code> : 返回一个指向map尾部的逆向迭代器</li>
<li><code>rend()</code> : 返回一个指向map头部的逆向迭代器</li>
<li><code>size()</code> : 返回map中元素的个数</li>
<li><code>swap()</code> : 交换两个map</li>
<li><code>upper_bound()</code> : 返回键值&gt;给定元素的第一个位置</li>
<li><code>value_comp()</code> : 返回比较元素value的函数</li>
</ul>
<h2 id="string"><a href="#string" class="headerlink" title="string"></a>string</h2><h3 id="1-string构造函数"><a href="#1-string构造函数" class="headerlink" title="1.string构造函数"></a>1.string构造函数</h3><ul>
<li><code>string();</code>&#x2F;&#x2F;创建一个空的字符串</li>
<li><code>string(const string&amp; str);</code>&#x2F;&#x2F;使用一个string对象初始化另一个string对象</li>
<li><code>string(const char* s);</code>&#x2F;&#x2F;使用字符串s初始化</li>
<li><code>string(int n,char c);</code>&#x2F;&#x2F;使用n个字符c初始化</li>
</ul>
<h3 id="2-string基本赋值操作"><a href="#2-string基本赋值操作" class="headerlink" title="2.string基本赋值操作"></a>2.string基本赋值操作</h3><ul>
<li><code>string&amp; operator=(const char* s);</code>&#x2F;&#x2F;char*类型字符串 赋值给当前的字符串</li>
<li><code>string&amp; operator=(const string &amp;s);</code>&#x2F;&#x2F;把字符串s赋值给当前的字符串</li>
<li><code>string&amp; operator=(char c);</code>&#x2F;&#x2F;字符赋值给当前的字符串</li>
<li><code>string&amp; assign(const char* s);</code>&#x2F;&#x2F;把字符串s赋值给当前的字符串</li>
<li><code>string&amp; assign(const char* s,int n);</code>&#x2F;&#x2F;把字符串s的前n个字符赋值给当前的字符串</li>
<li><code>string&amp; assign(const string &amp;s);</code>&#x2F;&#x2F;把字符串s赋值给当前的字符串</li>
<li><code>string&amp; assign(int n,char c);</code>&#x2F;&#x2F;用n个字符c赋值给当前字符串</li>
<li><code>string&amp; assign(const string &amp;s,int start,int n);</code>&#x2F;&#x2F;将s从start开始n个字符赋值给字符串</li>
</ul>
<h3 id="3-string存取字符操作"><a href="#3-string存取字符操作" class="headerlink" title="3.string存取字符操作"></a>3.string存取字符操作</h3><ul>
<li><code>char&amp; operator[](int n);</code>&#x2F;&#x2F;通过<code>[]</code>方式取字符</li>
<li><code>char&amp; at(int n);</code>&#x2F;&#x2F;通过at方法获取字符</li>
</ul>
<h3 id="4-string拼接操作"><a href="#4-string拼接操作" class="headerlink" title="4.string拼接操作"></a>4.string拼接操作</h3><ul>
<li><code>string&amp; operator+=(const string&amp; str);</code>&#x2F;&#x2F;重载+&#x3D;运算符</li>
<li><code>string&amp; operator+=(const char* str);</code>&#x2F;&#x2F;重载+&#x3D;运算符</li>
<li><code>string&amp; operator+=(const char c);</code>&#x2F;&#x2F;重载+&#x3D;运算符</li>
<li><code>string&amp; append(const char *s);</code>&#x2F;&#x2F;把字符串s连接到当前字符串结尾</li>
<li><code>string&amp; append(const char *s,int n);</code>&#x2F;&#x2F;把字符串s的前n个字符连接到当前字符串结尾</li>
<li><code>string&amp; append(const string &amp;s);</code>&#x2F;&#x2F;同operator+&#x3D;（）</li>
<li><code>string&amp; append(const string &amp;s,int pos,int n);</code>&#x2F;&#x2F;把字符串s中从pos开始的n个字符连接到当前字符串结尾</li>
<li><code>string&amp; append(int n,char c);</code>&#x2F;&#x2F;在当前字符串结尾添加n个字符c</li>
</ul>
<h3 id="5-string查找和替换"><a href="#5-string查找和替换" class="headerlink" title="5.string查找和替换"></a>5.string查找和替换</h3><ul>
<li><code>int find(const string&amp; str,int pos = 0)const;</code>&#x2F;&#x2F;查找str第一次出现的位置，从pos开始查找</li>
<li><code>int find(const char* s,int pos = 0)const;</code>&#x2F;&#x2F;查找s第一次出现位置，从pos开始查找</li>
<li><code>int find(const char *s,int pos,int n)const;</code>&#x2F;&#x2F;从pos位置查找s的前n个字符第一次位置</li>
<li><code>int find(const char c,int pos = 0)const;</code>&#x2F;&#x2F;查找字符c第一次出现位置</li>
<li><code>int rfind(const string&amp; str, int pos = npos)const;</code>&#x2F;&#x2F;查找str最后一次出现位置，从pos开始查找</li>
<li><code>int rfind(const char* s,int pos = npos)const;</code>&#x2F;&#x2F;查找s最后一次出现位置，从pos开始查找</li>
<li><code>int rfind(const char* s,int pos,int n )const;</code>&#x2F;&#x2F;从pos查找s的前n个字符最后一次位置</li>
<li><code>int rfind(const char c,int pos = 0)const;</code>&#x2F;&#x2F;查找字符c最后一次出现位置</li>
<li><code>string&amp; replace(int pos,int n,const string&amp; str);</code>&#x2F;&#x2F;替换从pos开始的n个字符为字符串str</li>
<li><code>string&amp; replace(int pos,int n,const char* s);</code>&#x2F;&#x2F;替换从pos开始的n个字符为字符串s</li>
</ul>
<h3 id="6-string-比较操作"><a href="#6-string-比较操作" class="headerlink" title="6.string 比较操作"></a>6.string 比较操作</h3><ul>
<li><code>int compare(const string &amp;s)const;</code>&#x2F;&#x2F;与字符串s比较</li>
<li><code>int compare(const char*s)const;</code>&#x2F;&#x2F;与字符串s比较</li>
<li>compare函数在&gt;时返回1，&lt;时返回-1，相等时返回0，比较区分大小写，逐个字符比较</li>
</ul>
<h3 id="7-string子串"><a href="#7-string子串" class="headerlink" title="7.string子串"></a>7.string子串</h3><ul>
<li><code>string substr(int pos = 0,int n = npos)const;</code>&#x2F;&#x2F;返回由pos开始的n个字符组成的字符串</li>
</ul>
<h3 id="8-string插入和删除操作"><a href="#8-string插入和删除操作" class="headerlink" title="8.string插入和删除操作"></a>8.string插入和删除操作</h3><ul>
<li><code>string&amp; insert(int pos,const char* s);</code>&#x2F;&#x2F;插入字符串</li>
<li><code>string&amp; insert(int pos,const string &amp;str);</code>&#x2F;&#x2F;插入字符串</li>
<li><code>string&amp; insert(int pos,int n,char c);</code>&#x2F;&#x2F;在指定位置插入n个字符c</li>
<li><code>string&amp; erase(int pos,int n = npos);</code>&#x2F;&#x2F;删除从pos开始的n个字符</li>
</ul>
<h2 id="deque"><a href="#deque" class="headerlink" title="deque"></a>deque</h2><p>deque是Double-Ended Queues(双向队列)的缩写。</p>
<p>双向队列和向量很相似，但是它允许在容器头部快速插入和删除（就像在尾部一样）。</p>
<h3 id="1-deque构造函数"><a href="#1-deque构造函数" class="headerlink" title="1.deque构造函数"></a>1.deque构造函数</h3><ul>
<li><code>deque&lt;T&gt; queT;</code>&#x2F;&#x2F;queue采用模板类实现，queue对象的默认构造形式</li>
<li><code>deque&lt;T&gt; queT(size);</code>&#x2F;&#x2F;构造大小为size的deque，其中值为T类型的默认值</li>
<li><code>deque&lt;T&gt; queT(size, val);</code>&#x2F;&#x2F;构造大小为size的deque，其中值为val</li>
<li><code>deque(const deque &amp;que);</code>&#x2F;&#x2F;拷贝构造函数</li>
<li><code>deque(input_iterator start, input_iterator end);</code>&#x2F;&#x2F;迭代器构造函数</li>
</ul>
<p>example:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// constructing deques</span><br><span class="line">#include &lt;iostream&gt;</span><br><span class="line">#include &lt;deque&gt;</span><br><span class="line"></span><br><span class="line">int main ()</span><br><span class="line">&#123;</span><br><span class="line">  unsigned int i;</span><br><span class="line"></span><br><span class="line">  // constructors used in the same order as described above:</span><br><span class="line">  std::deque&lt;int&gt; first;                                // empty deque of ints</span><br><span class="line">  std::deque&lt;int&gt; second (4,100);                       // four ints with value 100</span><br><span class="line">  std::deque&lt;int&gt; third (second.begin(),second.end());  // iterating through second</span><br><span class="line">  std::deque&lt;int&gt; fourth (third);                       // a copy of third</span><br><span class="line"></span><br><span class="line">  // the iterator constructor can be used to copy arrays:</span><br><span class="line">  int myints[] = &#123;16,2,77,29&#125;;</span><br><span class="line">  std::deque&lt;int&gt; fifth (myints, myints + sizeof(myints) / sizeof(int) );</span><br><span class="line"></span><br><span class="line">  std::cout &lt;&lt; &quot;The contents of fifth are:&quot;;</span><br><span class="line">  for (std::deque&lt;int&gt;::iterator it = fifth.begin(); it!=fifth.end(); ++it)</span><br><span class="line">    std::cout &lt;&lt; &#x27; &#x27; &lt;&lt; *it;</span><br><span class="line"></span><br><span class="line">  std::cout &lt;&lt; &#x27;\n&#x27;;</span><br><span class="line"></span><br><span class="line">  return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Output:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">The contents of fifth are: 16 2 77 29 </span><br></pre></td></tr></table></figure>

<h3 id="2-deque存取、插入和删除操作"><a href="#2-deque存取、插入和删除操作" class="headerlink" title="2.deque存取、插入和删除操作"></a>2.deque存取、插入和删除操作</h3><ul>
<li><code>back();</code>&#x2F;&#x2F;返回最后一个元素</li>
<li><code>front();</code>&#x2F;&#x2F;返回第一个元素</li>
<li><code>insert();</code>&#x2F;&#x2F;</li>
<li><code>pop_back();</code>&#x2F;&#x2F;删除尾部的元素</li>
<li><code>pop_front();</code>&#x2F;&#x2F;删除头部的元素</li>
<li><code>push_back();</code>&#x2F;&#x2F;在尾部加入一个元素</li>
<li><code>push_front();</code>&#x2F;&#x2F;在头部加入一个元素</li>
<li><code>at();</code>&#x2F;&#x2F;访问指定位置元素</li>
</ul>
<blockquote>
<p>注：queue只有push和pop方法，在尾部加入和删除元素。</p>
</blockquote>
<h3 id="3-deque赋值操作"><a href="#3-deque赋值操作" class="headerlink" title="3.deque赋值操作"></a>3.deque赋值操作</h3><ul>
<li><code>operator[] (size_type n);</code>&#x2F;&#x2F;重载[]操作符</li>
</ul>
<h3 id="4-deque大小操作"><a href="#4-deque大小操作" class="headerlink" title="4.deque大小操作"></a>4.deque大小操作</h3><ul>
<li><code>empty()</code>;&#x2F;&#x2F;判断队列是否为空</li>
<li><code>size()</code>;&#x2F;&#x2F;返回队列的大小</li>
</ul>
<h2 id="priority-queue"><a href="#priority-queue" class="headerlink" title="priority_queue"></a>priority_queue</h2><p>优先队列类似队列， 但是在这个数据结构中的元素按照一定的规则排列有序。</p>
<p>在优先队列中，元素被赋予优先级。当访问元素时，具有最高优先级的元素最先删除。优先队列具有<strong>最高优先级先出</strong> （first in, largest out）的行为特征。</p>
<p>首先要包含头文件#include, 他和queue不同的就在于我们可以自定义其中数据的优先级, 让优先级高的排在队列前面,优先出队。</p>
<p>优先队列具有队列的所有特性，包括队列的基本操作，只是在这基础上添加了内部的一个排序，它<strong>本质是一个堆实现的</strong>。</p>
<p>构造函数： <code>priority_queue&lt;Type, Container, Functional&gt;</code></p>
<ul>
<li>Type 就是数据类型，</li>
<li>Container 就是容器类型（Container必须是具备<strong>随机存取</strong>能力的容器，支持如下方法：<code>empty()</code>, <code>size()</code>, <code>front()</code>, <code>push_back()</code>,<code>pop_back()</code>。比如vector,deque等等，但不能用list。STL里面默认用的是vector）。可选</li>
<li>Functional 就是比较的方式。可选</li>
</ul>
<p>当需要用自定义的数据类型时才需要传入这三个参数，使用基本数据类型时，只需要传入数据类型，默认是大顶堆(Functional是less)。</p>
<p>example:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// constructing priority queues</span><br><span class="line">#include &lt;iostream&gt;       // std::cout</span><br><span class="line">#include &lt;queue&gt;          // std::priority_queue</span><br><span class="line">#include &lt;vector&gt;         // std::vector</span><br><span class="line">#include &lt;functional&gt;     // std::greater</span><br><span class="line"></span><br><span class="line">class mycomparison</span><br><span class="line">&#123;</span><br><span class="line">  bool reverse;</span><br><span class="line">public:</span><br><span class="line">  mycomparison(const bool&amp; revparam=false)</span><br><span class="line">    &#123;reverse=revparam;&#125;</span><br><span class="line">  bool operator() (const int&amp; lhs, const int&amp;rhs) const</span><br><span class="line">  &#123;</span><br><span class="line">    if (reverse) return (lhs&gt;rhs);</span><br><span class="line">    else return (lhs&lt;rhs);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">int main ()</span><br><span class="line">&#123;</span><br><span class="line">  int myints[]= &#123;10,60,50,20&#125;;</span><br><span class="line"></span><br><span class="line">  std::priority_queue&lt;int&gt; first;</span><br><span class="line">  std::priority_queue&lt;int&gt; second (myints,myints+4);</span><br><span class="line">  std::priority_queue&lt;int, std::vector&lt;int&gt;, std::greater&lt;int&gt; &gt;</span><br><span class="line">                            third (myints,myints+4);</span><br><span class="line">  // using mycomparison:</span><br><span class="line">  typedef std::priority_queue&lt;int,std::vector&lt;int&gt;,mycomparison&gt; mypq_type;</span><br><span class="line"></span><br><span class="line">  mypq_type fourth;                       // less-than comparison</span><br><span class="line">  mypq_type fifth (mycomparison(true));   // greater-than comparison</span><br><span class="line"></span><br><span class="line">  return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>The example does not produce any output, but it constructs different priority_queue objects: - First is empty. - Second contains the four ints defined for myints, with 60 (the highest) at its top. - Third has the same four ints, but because it uses greater instead of the default (which is less), it has 10 as its top element. - Fourth and fifth are very similar to first: they are both empty, except that these use mycomparison for comparisons, which is a special stateful comparison function that behaves differently depending on a flag set on construction.</p>
</blockquote>
<p>api和队列基本操作相同:</p>
<ul>
<li><code>top</code> 访问队头元素</li>
<li><code>empty</code> 队列是否为空</li>
<li><code>size</code> 返回队列内元素个数</li>
<li><code>push</code> 插入元素到队尾 (并排序)</li>
<li><code>emplace</code> 原地构造一个元素并插入队列</li>
<li><code>pop</code> 弹出队头元素</li>
<li><code>swap</code> 交换内容</li>
</ul>
]]></content>
      <categories>
        <category>coding</category>
      </categories>
      <tags>
        <tag>编程</tag>
      </tags>
  </entry>
  <entry>
    <title>Git学习及本地部署</title>
    <url>/posts/607d6c62/</url>
    <content><![CDATA[<h2 id="Git是什么？"><a href="#Git是什么？" class="headerlink" title="Git是什么？"></a>Git是什么？</h2><p>Git是目前世界上最先进的分布式版本控制系统。</p>
<p>自动记录每次文件的改动，可以让同事协作编辑。如果想查看某次改动，只需要在软件里瞄一眼就可以。</p>
<span id="more"></span>

<h2 id="1-安装Git"><a href="#1-安装Git" class="headerlink" title="1.安装Git"></a>1.安装Git</h2><h3 id="在Linux上安装Git"><a href="#在Linux上安装Git" class="headerlink" title="在Linux上安装Git"></a>在Linux上安装Git</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sudo apt-get install git</span><br></pre></td></tr></table></figure>

<h3 id="在Mac-OS-X上安装Git"><a href="#在Mac-OS-X上安装Git" class="headerlink" title="在Mac OS X上安装Git"></a>在Mac OS X上安装Git</h3><p>通过homebrew安装Git</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">brew install git</span><br></pre></td></tr></table></figure>

<h2 id="2-配置Git用户信息"><a href="#2-配置Git用户信息" class="headerlink" title="2.配置Git用户信息"></a>2.配置Git用户信息</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git config --global user.name   &quot;你的名字或昵称&quot;</span><br><span class="line">git config --global user.email  &quot;你的邮箱&quot;</span><br></pre></td></tr></table></figure>

<h2 id="3-创建版本库"><a href="#3-创建版本库" class="headerlink" title="3.创建版本库"></a>3.创建版本库</h2><p>版本库又名仓库，英文名<strong>repository</strong>，你可以简单理解成一个目录，这个目录里面的所有文件都可以被Git管理起来，每个文件的修改、删除，Git都能跟踪，以便任何时刻都可以追踪历史，或者在将来某个时刻可以“还原”。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">cd </span><br><span class="line">mkdir git</span><br><span class="line">cd git</span><br><span class="line">pwd</span><br></pre></td></tr></table></figure>

<p>第二步，通过<code>git init</code>命令把这个目录变成Git可以管理的仓库：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git init</span><br></pre></td></tr></table></figure>

<h2 id="4-文件放到Git仓库"><a href="#4-文件放到Git仓库" class="headerlink" title="4.文件放到Git仓库"></a>4.文件放到Git仓库</h2><p>第一步，用命令<code>git add</code>告诉Git，把文件添加到仓库：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git add readme.txt</span><br></pre></td></tr></table></figure>

<p>第二步，用命令<code>git commit</code>告诉Git，把文件提交到仓库：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git commit -m &quot;wrote a readme file&quot;</span><br></pre></td></tr></table></figure>

<p><code>git commit</code>命令，<code>-m</code>后面输入的是本次提交的说明，可以输入任意内容，当然最好是有意义的，这样你就能从历史记录里方便地找到改动记录。</p>
<h2 id="5-常用命令"><a href="#5-常用命令" class="headerlink" title="5.常用命令"></a>5.常用命令</h2><p><code>git status</code>命令可以让我们时刻掌握仓库当前的状态</p>
<p><code>git diff</code>顾名思义就是查看difference</p>
<p><code>git log</code>命令显示从最近到最远的提交日志</p>
<p><code>git reset --hard commit_id</code>：回退指定版本</p>
<p><code>git reflog</code>用来记录你的每一次命令</p>
<p><code>git checkout</code>其实是用版本库里的版本替换工作区的版本，无论工作区是修改还是删除，都可以“一键还原”。</p>
<p><code>git rm</code>用于删除一个文件。如果一个文件已经被提交到版本库，那么你永远不用担心误删，但是要小心，你只能恢复文件到最新版本，你会丢失<strong>最近一次提交后你修改的内容</strong>。</p>
]]></content>
      <categories>
        <category>git</category>
      </categories>
      <tags>
        <tag>杂谈</tag>
      </tags>
  </entry>
  <entry>
    <title>svm原理初步学习和数学公式推导</title>
    <url>/posts/447d0726/</url>
    <content><![CDATA[<p>支持向量机（英语：support vector machine，常简称为SVM，又名支持向量网络）是一种二类分类模型，他的基本模型是的定义在特征空间上的<strong>间隔最大</strong>的线性分类器，SVM的学习策略就是间隔最大化。</p>
<p>svm有三宝：间隔、对偶、核技巧</p>
<p>Svm:</p>
<ol>
<li>Hard-margin svm</li>
<li>Soft-margin svm</li>
<li>Kernel svm</li>
</ol>
<p>​	 </p>
]]></content>
      <categories>
        <category>machine learning</category>
      </categories>
      <tags>
        <tag>machine learning</tag>
      </tags>
  </entry>
  <entry>
    <title>余老师，我喜欢你</title>
    <url>/posts/1faadecf/</url>
    <content><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p>
<h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo new <span class="string">&quot;My New Post&quot;</span></span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p>
<span id="more"></span>

<h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p>
<h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p>
<h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>
]]></content>
      <tags>
        <tag>杂谈</tag>
      </tags>
  </entry>
  <entry>
    <title>【文献阅读】如何阅读科研论文</title>
    <url>/posts/fd318370/</url>
    <content><![CDATA[<h2 id="1-引言"><a href="#1-引言" class="headerlink" title="1.引言"></a>1.引言</h2><p>科研人员必须阅读论文的几个理由：为了会议或者学生审稿、为了在本领域中保持消息灵通、为了解新领域进行文献调研。一个典型的科研人员每年可能要花费几百小时去阅读论文。</p>
<p>学会有效地阅读论文是一项非常重要但是很少会有机会学到的技能。正因为这样，新入学的研究生都是在“试验——失败”中独自学习的。学生们会因此浪费大量的时间，并且会经常因此感到沮丧。</p>
<span id="more"></span>

<p>多年来，我一直使用一个简单的“三步法”来使我在读论文时能对其有居高临下的理解，而不是淹没在细节当中。这种读论文的方法还可以让我评估审阅一堆论文时需要花费的时间。除此之外，我可以根据需要和自己时间的多少来调整对论文的理解深度。这篇文章描述了“三步法”的具体内容和该方法在做文献调研时应如何应用。</p>
<h2 id="2-三步法"><a href="#2-三步法" class="headerlink" title="2.三步法"></a>2.三步法</h2><p>​		主要思想就是你应该在三步内读完论文，而不是从开始不分轻重地“耕耘”到最后。每一步要基于之前的阅读完成一个明确的目标：第一步要弄清一篇论文的大体想法；第二步要抓住论文的主要内容，但不是细节；第三步帮助你深度理解整篇论文。</p>
<h3 id="2-1第一步"><a href="#2-1第一步" class="headerlink" title="2.1第一步"></a>2.1第一步</h3><p>第一步是：快速浏览整篇论文，对其有个大概的了解。你也可以自己决定是否需要进一步阅读。这一步大概需要五到十分钟，有下面几个小节组成：</p>
<ol>
<li>认真阅读题目、摘要、引言。</li>
<li>阅读标题和段落开头，除此之外一概不看。</li>
<li>瞥一眼数学部分（如果有的话），以确定其基本理论概况。</li>
<li>阅读结论部分。</li>
<li>瞥一眼引用，找一下是否有你读过的论文。</li>
</ol>
<p>完成第一步后，你应该能回答以下几个问题了：</p>
<p>1．类别：这篇论文是什么类型？测试类？对现有系统进行分析的？对原型系统进行描述的？</p>
<p>2．内容：你读过的其他论文有没有和这个相关的？文章中分析问题用的什么理论基础？</p>
<p>3．正确性：结论看起来是否真实有效？</p>
<p>4．创新点：论文的主要创新点是什么？</p>
<p>5．清晰度：这篇论文是否写的条理清晰？</p>
<p>得出以上信息后，你可以选择不再继续阅读（那就不打印，也就节约了树木:））。这有可能是因为这篇论文不吸引你，或者你对这篇论文所涉及的领域了解不多，也可能因为作者给出了不适当的假设。这一步对于处理当前不在你研究领域内，但是以后也许相关的论文非常有效。</p>
<p>顺带提一下，当你写一篇论文时，你可以设想大部分的审稿人（或读者）只对你的论文使用第一步。小心地选择标题和子段落开头部分，摘要应该精确而且易理解。如果一个审稿人在第一步过后还不能理解你论文的主旨，那么很可能你的论文就不过关了。如果一个读者在五分钟内不能理解论文的突出部分，那么很可能以后这篇论文再也不会被捡起。正是由于这些原因，用一个“图示摘要”及一个精心选择的图形总结一篇论文是很棒的想法，以后会在科学杂志中越来越多。</p>
<h3 id="2-2第二步"><a href="#2-2第二步" class="headerlink" title="2.2第二步"></a>2.2第二步</h3><p>在第二步中，要仔细阅读论文，但是诸如证明等信息要忽略掉。在阅读时你应该草记下重点，或者在论文空白处写点标注。来自奥格斯堡（Augsburg）的Dominik Grusemann建议“记录下那些你不懂的部分，或者那些你想问作者的问题”。如果你是一名论文评审员，以上那些记录在你写评审意见时很有用，也可以在开论文研讨会时帮助你记录你当时的想法。</p>
<ol>
<li>仔细论文中阅读图片、表格和其他说明。要特别注意图表：坐标轴标记是否合适？标示的结果是否有误差线？这类常见错误可以区分那些粗制滥造的论文和真正出色的论文。</li>
<li>为了进一步阅读相关论文，记得要标注相关的未读引用文献（这是进一步了解论文相关背景的好方法）。</li>
</ol>
<p>第二步对于有经验的读者来说，应该需要一小时左右的时间。经过以上步骤，你应该就了解了论文的整体内容。此时你应该能把论文的主要思想和相关论据讲给其他人听了。对于你感兴趣的论文，读到这个程度就可以了，但是对于那些在你研究领域内的论文来时，还远远不够。</p>
<p>有些论文，你甚至可能在第二步结束的时候还不能理解。这有可能是因为论文中的主旨对你来说是全新的，论文里的术语和缩写词你都陌生，也可能是因为作者用了你不理解的论据和试验技术，所以导致论文中大部分的内容都不好理解。有些论文有可能写的很差，充斥着未经证实的断言和大量前向引用（forward references）。不过也可能你是在夜里很累的情况下阅读的，因此感觉不太好懂，如果是这样，你可以选择：（1）把论文放到一边，期望着在你走向事业成功的路上不需要理解它，（b）在了解了相关背景知识后，过段时间再回头读，或者（c）坚持不懈继续阅读。</p>
<h3 id="2-3第三步"><a href="#2-3第三步" class="headerlink" title="2.3第三步"></a>2.3第三步</h3><p>为了彻底理解一篇论文，尤其是当你是审稿人时，必须要走到第三步。第三部的核心思想是，试着“在脑中重新实现（virtually re-implement）”那篇论文：也就是，和作者一样做出相同的假设，然后重新实现相同的工作。通过对比你自己得出的和论文中给出的结果，不仅可以很轻易验证一篇论文的创新点是否真实，而且还能发现论文中不会讲的缺点和假想。</p>
<p>这一步需要耗费你很多精力去细化，完成这一步，你就可以验证论文中的每一个假设是否真实。而且，同时要思考下，如果你是作者，会如何呈现并介绍自己的想法。这种虚拟和现实的对比，可以让你对论文中的论据和表达技巧有深刻的理解，当然就可以把这种表达技巧和论据“据为己有”。在完成第三步过程中，你应该时刻不忘记下对于未来研究工作的新想法。</p>
<p>这一步有可能需要耗费初学者很多小时，对于有经验的研究人员也要至少一两个小时。当完成这一步后，你应该可以在脑海中重现构造论文的整个架构，同时能确定论文中哪些是好的，哪些是一般的论点。特别地，你也应该能明确指出论文中不清晰的假设、漏掉引用的相关工作以及论文中试验和分析的手段潜在的缺点。</p>
<h2 id="3-如何完成文献调研"><a href="#3-如何完成文献调研" class="headerlink" title="3.如何完成文献调研"></a>3.如何完成文献调研</h2><p>做文献调研就是考验你读论文技巧的时候了，调研工作有可能需要你阅读几十篇论文，同时也可能是你不熟悉的领域。什么样的论文值得一读？以下就是如何使用“三步法”帮你调研文献。</p>
<p>首先，使用学术搜索引擎例如Google Scholar或者CiteSeer，并且精心选择搜索词，找出三到五篇领域中“最近最多被引用”的论文。对于以上搜到的论文，使用三步法中的第一步，可以使自己对于相关工作有点感觉，然后阅读它们的相关工作章节。你会对于最近的相关工作有缩略图似的总结，如果你足够幸运的话，有可能在以上相关工作中找到近期发表的调研论文。如果你能找到类似的调研论文，调研工作就完成了！认真阅读那篇调研文献，庆祝自己的运气吧。</p>
<p>不然，在第二步中，找到以上几篇论文中相同的引用文献、在引用文献中重复的作者名，这些就是你所调研领域的重点论文和科研人员，下载那些重点论文暂时搁到一边。继续访问那些重要科研人员的网页，看下那些论文是在什么会议集结出版的。这能帮你找到那个领域的顶级会议，因为那些重要科研人员通常会在顶级会议发表论文。</p>
<p>第三步就是访问那些顶级会议的网站，查看一下他们最近的活动。快速扫描一眼就能找到最近的高质量相关论文，这些论文和上面提到暂时放到一边的论文，会对你文献调研工作的第一版本有所帮助。对于这些论文，使用“三步法”中的第二步，如果它们都引用了一篇重要文献，恰巧是你没有搜到的，果断下载并阅读。如果需要，重复以上工作。</p>
<h2 id="4-相关工作"><a href="#4-相关工作" class="headerlink" title="4.相关工作"></a>4.相关工作</h2><p>如果你是为了审稿而读论文，Timothy Roscoe的论文《Writing reviews for systems conferences》可以参考。如果你的计划是写一篇技术性论文，Henning Schulzrinne的综合网页和George Whitesides的过程概述可以参考。最后，Simon Peyton Jones有一个网页涵盖了所有实验技巧。</p>
<p>心理学领域的Iain H. McLean打包发布了一个可下载的“评审矩阵”，介绍了如何用“三步法”简化在实验心理学领域进行论文评审（你有可能用到），以及对于其他领域的论文如何使用“微调三步法”。</p>
<h2 id="6-文献来源"><a href="#6-文献来源" class="headerlink" title="6.文献来源"></a>6.文献来源</h2><p><a href="https://dl.acm.org/doi/10.1145/1273445.1273458"><em>How to read a paper</em></a></p>
]]></content>
      <tags>
        <tag>文献阅读</tag>
      </tags>
  </entry>
  <entry>
    <title>数据库速成</title>
    <url>/posts/b1aeb731/</url>
    <content><![CDATA[<h2 id="第一章-绪论"><a href="#第一章-绪论" class="headerlink" title="第一章 绪论"></a>第一章 绪论</h2><h3 id="1、数据和语义的关系？"><a href="#1、数据和语义的关系？" class="headerlink" title="1、数据和语义的关系？"></a>1、数据和语义的关系？</h3><p>描述事物的符号记录称为数据。<br>数据的含义称为数据语义，数据与其语义是不可分的。</p>
<h3 id="2、区分数据库、数据库系统、数据库管理系统的概念"><a href="#2、区分数据库、数据库系统、数据库管理系统的概念" class="headerlink" title="2、区分数据库、数据库系统、数据库管理系统的概念"></a>2、区分数据库、数据库系统、数据库管理系统的概念</h3><p>数据库（DataBase，DB）：<br>数据库是长期储存在计算机内、有组织的、可共享的大量数据的集合。</p>
<p>数据库管理系统（DataBase Management System，DBMS）：<br>数据库管理系统是位于用户与操作之间的一层数据管理软件。</p>
<p>数据库系统（DataBase System，DBS）：<br>数据库系统是由数据库、数据库管理系统（及其应用开发工具）、应用程序和数据库管理员组成的存储、管理、处理和维护数据的系统。</p>
<h3 id="3、数据库管理系统的功能有哪些？"><a href="#3、数据库管理系统的功能有哪些？" class="headerlink" title="3、数据库管理系统的功能有哪些？"></a>3、数据库管理系统的功能有哪些？</h3><p>数据定义功能<br>数据组织、存储和管理<br>数据操纵功能<br>数据库的事务和运行管理<br>数据库的建立和维护功能</p>
<p>其他功能</p>
<h3 id="4、数据库系统的特点？有哪些主流的数据库管理系统？"><a href="#4、数据库系统的特点？有哪些主流的数据库管理系统？" class="headerlink" title="4、数据库系统的特点？有哪些主流的数据库管理系统？"></a>4、数据库系统的特点？有哪些主流的数据库管理系统？</h3><p>数据库系统的特点：</p>
<p>数据结构化<br>数据的共享性高、冗余度低且易扩展<br>数据独立性高<br>数据由数据库管理系统统一管理和控制<br>目前互联网上常见的数据库管理系统有Oracle、MySQL、MS SQL Server、DB2、PostgreSQL、Access、Sybase、Informix</p>
<h3 id="5、什么是数据独立性、物理独立性、逻辑独立性？"><a href="#5、什么是数据独立性、物理独立性、逻辑独立性？" class="headerlink" title="5、什么是数据独立性、物理独立性、逻辑独立性？"></a>5、什么是数据独立性、物理独立性、逻辑独立性？</h3><p>数据独立性包括数据的物理独立性和数据的逻辑独立性。</p>
<p>物理独立性是指用户的应用程序与数据库中数据的物理存储是相互独立的<br>逻辑独立性是指用户的应用程序与数据库的逻辑结构是相互独立的</p>
<h3 id="6、数据的不一致性、安全性、完整性、并发控制、恢复的概念？数据模型的三要素？"><a href="#6、数据的不一致性、安全性、完整性、并发控制、恢复的概念？数据模型的三要素？" class="headerlink" title="6、数据的不一致性、安全性、完整性、并发控制、恢复的概念？数据模型的三要素？"></a>6、数据的不一致性、安全性、完整性、并发控制、恢复的概念？数据模型的三要素？</h3><p>不一致性：同一数据不同副本的值不一样<br>安全性（security）：保护数据以防止不合法使用造成的数据泄露<br>完整性（integrity）：数据的正确性、有效性和相容性。<br>并发（concurrency）控制：当多个用户的并发进程同时存取、修改数据库时，可能会发生相互干扰而得到错误的结果或使数据库的完整性遭到破坏，因此必须对多用户的并发操作加以控制和协调<br><strong>数据库恢复（recovery）：</strong>将数据库从错误状态恢复到某一已知的正确状态</p>
<p>数据模式的三要素：</p>
<ol>
<li>数据结构：描述数据库的组成对象以及对象之间的联系</li>
<li>数据操作：对数据库中各种对象（型）和实例（值）允许执行的操作的集合，包括操作及有关操作的规则</li>
<li>数据的完整性约束：一组完整性规则</li>
</ol>
<h3 id="7、概念模型、模型"><a href="#7、概念模型、模型" class="headerlink" title="7、概念模型、模型"></a>7、概念模型、模型</h3><p>概念模型：<br>1、信息世界中的基本概念</p>
<p>实体（entity）<br>客观存在并可互相区别的事物叫做实体<br>属性（attribute）<br>实体所具有的某一特征称为属性。<br>码（key）<br>唯一标识实体的属性集称为码<br>实体型（entity type）<br>用实体名称及其属性名集合来抽象和刻画同类实体，称为实体型<br>实体集（entity set）<br>同一类型实体的集合称为实体集。例如全体学生就是一个实体集<br>联系（relationship）<br>实体之间的联系通常是指不同实体集之间的联系。<br>实体之间的联系有一对一、一对多和多对多等多种类型。<br>2、概念模型的一种表示方法：实体-联系方法<br>实体-联系方法（E-R方法）</p>
<h3 id="8、层次模型、网状模型，关系模型的概念"><a href="#8、层次模型、网状模型，关系模型的概念" class="headerlink" title="8、层次模型、网状模型，关系模型的概念"></a>8、层次模型、网状模型，关系模型的概念</h3><p><strong>层次模型：</strong><br>（1）有且有一个结点没有双亲结点，这个结点称为根节点<br>（2）根以外的其他结点有且只有一个双亲结点</p>
<img src="https://raw.githubusercontent.com/leoz9/picture/main/blog/202304081420645.png" alt="15bd2a382b9e45348d5f59a0ccb15bef" style="zoom:70%;" />

<p><strong>网状模型：</strong><br>（1）允许一个以上的结点无双亲<br>（2）一个结点可以有多个双亲</p>
<img src="https://raw.githubusercontent.com/leoz9/picture/main/blog/202304081420725.png" alt="2f19722a2cb045b38d39e9577dea6ac5" style="zoom:67%;" />

<p><strong>关系模型</strong><br>（1）关系模型的概念建立在严格的数学概念基础上<br>（2）从用户观点来看，关系模型由一组关系组成。每个关系的数据结构是一张规范化的二维表<img src="https://raw.githubusercontent.com/leoz9/picture/main/blog/202304081421101.png" alt="f80bfb16fc3a4aea909ad55b16f297f6"></p>
<h3 id="9、关系模型中的术语：关系、元组、属性、主码、外码、全码、域、分量、关系模式"><a href="#9、关系模型中的术语：关系、元组、属性、主码、外码、全码、域、分量、关系模式" class="headerlink" title="9、关系模型中的术语：关系、元组、属性、主码、外码、全码、域、分量、关系模式"></a>9、关系模型中的术语：关系、元组、属性、主码、外码、全码、域、分量、关系模式</h3><p>关系（relation）：一个关系对应通常说的一张表。如上图的这张学生登记表<br>元组（tuple）：表中的一行即为一个元组<br>属性（attribute）：表中的一列即为一个属性，给每一个属性起一个名字即属性名。如上图所示的表有6列，对应6个属性（学号，姓名，年龄，性别，系名和年级）<br>主码(primary key) 一般指主关键字。 主关键字是表中的一个或多个字段，它的值用于唯一的标识表中的某一条记录。<br><strong>外码：</strong>数据库中如果一个关系中的一个属性是另外一个关系中的主码则这个属性为外码。<br><strong>全码：</strong>全码：当所有的属性共同构成一个候选码时，这时该候选码为全码。（教师，课程，学生）假如一个教师可以讲授多门课程，某门课程可以有多个教师讲授，学生可以听不同教师讲授的不同课程，那么，要区分关系中的每一个元组，这个关系模式R的候选码应为全部属性构成 （教师、课程、学生），即主码。<br>域（domain）：域是一组具有相同数据类型的值的集合。属性的取值范围来自某个域。如人的年龄一般在1~120岁之间，大学生年龄属性的域是（15-45岁），性别的域是（男，女），系名的域是一个学校所有姓名的集合<br>关系模式：对关系的描述，一般表示为<br>关系名（属性1，属性2，…,属性n）<br>例如 学生（学号，姓名，年龄，性别，系名，年级）</p>
<h3 id="10、什么是数据库系统的三级模式和两级映射？其作用是什么？"><a href="#10、什么是数据库系统的三级模式和两级映射？其作用是什么？" class="headerlink" title="10、什么是数据库系统的三级模式和两级映射？其作用是什么？"></a>10、什么是数据库系统的三级模式和两级映射？其作用是什么？</h3><p>1、数据库系统的三级模式结构是指数据库系统是由外模式、模式和内模式三级构成<br>（1）模式（schema）<br>模式也称为逻辑模式，是数据库中全体数据的逻辑结构和特征的描述，是所有用户的公共数据视图。<br>（2）外模式（external schema）<br>外模式也称为子模式（subschema）或用户模式，能够看见和使用局部数据的逻辑结构和特征描述，是数据库用户的数据视图，是与某一应用右端的数据的逻辑表示。<br>（3）内模式（internal schema）<br>内模式也称为存储模式（storage schema），一个数据库只有一个内模式。它是数据物理结构和存储方式的描述，是数据在数据库内部的组织方式。</p>
<p>作用：把数据的具体组织留给数据库管理系统，使用户能逻辑地、抽象地处理数据，而不必关心数据在计算机中的具体表现方法与存储方式。<img src="https://raw.githubusercontent.com/leoz9/picture/main/blog/202304081422043.png" alt="02386093d60e447cb2a55525e3bbb05c"></p>
<p>2、数据库的两级映射：</p>
<p>（1）外模式&#x2F;模式映像<br>当模式改变时（如增加新的关系、新的属性、改变属性的数据类型等），由数据库管理员对各个外模式&#x2F;模式的映像作相应改变，可以使外模式保持不变。应用程序是依据数据的外模式编写的，从而因公程序不必修改，保证了数据与程序的逻辑独立性，简称数据的逻辑独立性。<br>（2）模式&#x2F;内模式映像<br>当数据库的存储结构改变时（例如选用了另一种存储结构），由数据库管理员对模式&#x2F;内模式印象作象形改变，可以是模式保持不变，从而应用程序也不必改变。保证了数据与程序的物理独立性，简称数据的物理独立性</p>
<h3 id="11、数据库系统的组成？"><a href="#11、数据库系统的组成？" class="headerlink" title="11、数据库系统的组成？"></a>11、数据库系统的组成？</h3><p>数据库系统一般由数据库、数据库管理系统（及其应用开发工具）、应用程序和数据库管理员构成。具体是指：</p>
<p>硬件平台及数据库<br>软件<br>人员</p>
<h3 id="12、数据库管理员的职责？"><a href="#12、数据库管理员的职责？" class="headerlink" title="12、数据库管理员的职责？"></a>12、数据库管理员的职责？</h3><p>决定数据库中的信息内容和结构<br>决定数据库的存储结构和存取策略<br>定义数据的安全性要求和完整性约束条件<br>监控数据库的使用和运行<br>数据库的改进和重组、重构</p>
<h2 id="第二章-关系模型"><a href="#第二章-关系模型" class="headerlink" title="第二章 关系模型"></a>第二章 关系模型</h2><h3 id="1、关系模型中的三类完整性约束的概念？"><a href="#1、关系模型中的三类完整性约束的概念？" class="headerlink" title="1、关系模型中的三类完整性约束的概念？"></a>1、关系模型中的三类完整性约束的概念？</h3><p>（1）实体完整性（entity integrity）<br>若属性（指一个或一组属性）A是基本关系R的主属性，则A不能取空值（null value）。所谓的空值就是“不知道”或“不存在”或“无意义”的值。<br>例如，学生（学号，姓名，性别，专业号，年龄）关系中学号为主码，则学号不能取空值</p>
<h3 id="关系代数中的基本运算"><a href="#关系代数中的基本运算" class="headerlink" title="关系代数中的基本运算"></a>关系代数中的基本运算</h3><p><img src="https://raw.githubusercontent.com/leoz9/picture/main/blog/202304081426477.png" alt="aed2147ee48e44239a0033890353362f"></p>
<img src="https://raw.githubusercontent.com/leoz9/picture/main/blog/202304081426853.jpeg" alt="27bced1b2ae64e46bbdec8c3d38e199" style="zoom:50%;" />

<h2 id="第三、四章-SQL"><a href="#第三、四章-SQL" class="headerlink" title="第三、四章 SQL"></a>第三、四章 SQL</h2><img src="https://raw.githubusercontent.com/leoz9/picture/main/blog/202304081427524.png" alt="f56a0dce399b45e8ac2b7bd886f15aa6" style="zoom:50%;" />

<h4 id="1、select"><a href="#1、select" class="headerlink" title="1、select"></a>1、select</h4><p>选择表中的若干列：</p>
<p>(1)查询指定列：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">select Sno,Sname from Student;</span><br></pre></td></tr></table></figure>

<p>(2)查询全部列：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">select * from student;</span><br></pre></td></tr></table></figure>

<p>(3)查询经过计算的值：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">select Sname,2021-Sage from Student;</span><br></pre></td></tr></table></figure>

<p>选择表中的若干元祖：</p>
<p>(1)消除取值重复的行：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">select distinct Sno from SC;</span><br></pre></td></tr></table></figure>

<p>(2)查询满足条件的元组：<br>1、比大小：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">select Sname from Student where Sage&gt;19;</span><br></pre></td></tr></table></figure>

<p>2、确定范围：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">select Sname,Sdept,Sage from Student where Sage between 18 and 19;</span><br></pre></td></tr></table></figure>

<p>3、确定集合：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">select Sname,Ssex from Student where Sdept in (&#x27;CS&#x27;,&#x27;MA&#x27;,&#x27;IS&#x27;);</span><br></pre></td></tr></table></figure>

<p>4、字符匹配：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">select * from Student where Sno like &#x27;201215121&#x27;;</span><br><span class="line">select Sname,Sno,Ssex from Student where Sname like &#x27;刘%&#x27;;</span><br><span class="line">select Sname from Student where Sname like &#x27;_阳%&#x27;;</span><br></pre></td></tr></table></figure>

<p>5、涉及空值的查询：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">select Sno,Cno from SC where Grade is not NULL;</span><br></pre></td></tr></table></figure>

<p>6、多重条件查询：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">select Sname from Student where Sdept=&#x27;CS&#x27; and Sage&lt;20;</span><br></pre></td></tr></table></figure>

<p>order by 子句：</p>
<p>用户可以用order by子句对查询结果安装一个或多个属性列的升序(ASC)或降序(DESC)排列，默认值为升序。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">select Sno,Grade from SC where Cno=&#x27;3&#x27; order by Grade desc;</span><br></pre></td></tr></table></figure>

<p>聚集函数：</p>
<p>为了进一步方便用户，增强检索功能，SQL提供了许多聚集函数，主要有：</p>
<p>count(*) 统计元祖个数</p>
<p>count([distinct|all]&lt;列名&gt;) 统计一列中值的个数</p>
<p>sum([distinct|all]&lt;列名&gt;) 计算一列值的总和(此列必须是数值型)</p>
<p>avg([distinct|all]&lt;列名&gt;) 计算一列值的平均值(此列必须是数值型)</p>
<p>max([distinct|all]&lt;列名&gt;) 求一列中的最大值</p>
<p>min([distinct|all]&lt;列名&gt;) 求一列中的最小值</p>
<p>如果指定distinct短语，则表示在计算时要取消指定列中的重复值。如果不指定distinct短语或指定all短语(all为默认值)，则表示不取消重复值。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">select count(*) from Student;</span><br><span class="line">select count(distinct Sno) from SC;</span><br><span class="line">select avg(Grade) from SC where Cno=&#x27;1&#x27;;</span><br><span class="line">select max(Grade) from SC where Cno=&#x27;1&#x27;;</span><br><span class="line">select min(Grade) from SC;</span><br></pre></td></tr></table></figure>

<p>group by子句</p>
<p>group by子句将查询结果按某一列或多列的值分组，值相等的为一组。对查询结果分组的目的是为了细化聚集函数的作用对象。分组后聚集函数将作用于每一个组，即每一组都有一个函数值。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">select Cno,count(Sno) from SC group by Cno;</span><br></pre></td></tr></table></figure>

<p>where子句与having短语的区别在于作用对象不同。where子句作用于基本表或视图，从中选择满足的元祖。having短语作用于组，从中选择满足条件的组。(group by与having搭配，不能与where搭配)</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">select Sno from SC group by Sno having count(*)&gt;2;</span><br></pre></td></tr></table></figure>

<p>多表查询太复杂了，直接看书吧。</p>
<h4 id="2、update"><a href="#2、update" class="headerlink" title="2、update"></a>2、update</h4><p>修改某一个元组的值：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">update Student set Sage=22 where Sno=&#x27;201215121&#x27;;</span><br></pre></td></tr></table></figure>

<p>修改多个元组的值：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">update Student set Sage=Sage+1;</span><br></pre></td></tr></table></figure>

<p>带子查询的修改语句：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">update SC set Grade=0 where Sno in (select Sno from Student where Sdept=&#x27;CS&#x27;);</span><br></pre></td></tr></table></figure>

<h4 id="3、alter-table"><a href="#3、alter-table" class="headerlink" title="3、alter table"></a>3、alter table</h4><p>alter table语句修改基本表，其一般格式为：</p>
<p>alter table&lt;表名&gt;</p>
<p>[add [column] &lt;新表名&gt;&lt;数据类型&gt; [完整性约束]]</p>
<p>[add [表级完整性约束]]</p>
<p>[drop [column] &lt;列名&gt; [cascade|restrict]]</p>
<p>[drop [column] &lt;完整性约束名&gt; [restrict|cascade]]</p>
<p>[alter column &lt;列名&gt;&lt;数据类型&gt;];</p>
<p>其中&lt;表名&gt;是要修改的基本表,add用于增加新列、新的列级完整性约束条件和新的表级完整性约束条件。drop column子句用于删除表中的列，如果指定了cascade短语，则自动删除引用该列的其他对象，比如视图；如果指定了restrict短语，则如果该列被其他对象引用，rdbms将拒绝删除该列。drop constrant子句用于删除指定的完整性约束条件。alter column子句用于修改原有的列定义，包括修改列名和数据类型。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">alter table Student add S_entrance date;</span><br></pre></td></tr></table></figure>

<h4 id="4、create-index"><a href="#4、create-index" class="headerlink" title="4、create index"></a>4、create index</h4><p>建立索引：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">create unique index Stusno on Student(Sno);</span><br></pre></td></tr></table></figure>

<p>unique表明此索引的每一个索引值只对应唯一的数据记录。</p>
<p>cluster表示要建立的是聚簇索引。</p>
<p>修改索引：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">alter index SCno rename to SCSno;</span><br></pre></td></tr></table></figure>

<p>删除索引：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">drop index SCno on `SC`; </span><br></pre></td></tr></table></figure>

<h4 id="5、insert"><a href="#5、insert" class="headerlink" title="5、insert"></a>5、insert</h4><p>插入元组：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">insert into Student (Sno,Sname,Ssex,Sdept,Sage) values(&#x27;201215128&#x27;,&#x27;陈冬&#x27;,&#x27;男&#x27;,&#x27;IS&#x27;,18);</span><br></pre></td></tr></table></figure>

<p>插入子查询结果：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">create table Dept_age(Sdept char(15),Avg_age int);</span><br><span class="line"></span><br><span class="line">insert into Dept_age(Sdept,Avg_age) select Sdept,avg(Sage) from Student Group by Sdept;</span><br></pre></td></tr></table></figure>

<h4 id="6、create-view"><a href="#6、create-view" class="headerlink" title="6、create view"></a>6、create view</h4><p>视图是从一个或几个基本表(或视图)导出的表。它与基本表不同，是一个虚表。数据库中只存放视图的定义，而不存在放视图对应的数据，这些数据仍存放在原来的基本表中。所以一旦基本表中的数据发生变化，从视图中查询出的数据也随之改变了。视图一经定义，就可以和基本表一样被查询、删除。也可以在视图之上再定义新的视图，但对视图的更新操作有一定的限制。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">create view is_student as select Sno,Sname,Sage from Student where Sdept=&#x27;IS&#x27;;</span><br></pre></td></tr></table></figure>

<p>定义视图时加上with check option子句，以后对该视图进行插入、修改和删除操作时，关系数据库管理系统会自动加上限制条件。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">create view as_student as select Sno,Sname,Sage from Student where Sdept=&#x27;IS&#x27; with check option;</span><br></pre></td></tr></table></figure>

<p>若一个视图时从单个基本表中导出，并且只是去掉了基本表的某些行和某些列，但是保留了主码，则称这类视图为行列子集视图。is_student视图就是一个行列子集视图。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">create view is_s1(Sno,Sname,Grade) as select Student.Sno,Sname,Grade from Student,SC where Sdept=&#x27;IS&#x27; and Student.Sno=Sc.Sno and SC.Cno=&#x27;1&#x27;;</span><br></pre></td></tr></table></figure>

<p>也可以建立在一个或多个已定义好的视图上，或建立在基本表与视图上。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">create view is_s2 as select Sno,Sname,Grade from is_s1 where grade&gt;=90;</span><br></pre></td></tr></table></figure>

<p>带虚拟列的视图也称为带表达式的视图。</p>
<h2 id="5、什么叫视图，视图的作用"><a href="#5、什么叫视图，视图的作用" class="headerlink" title="5、什么叫视图，视图的作用"></a>5、什么叫视图，视图的作用</h2><p><strong>视图的定义：</strong></p>
<p><img src="https://raw.githubusercontent.com/leoz9/picture/main/blog/202304081429951.png" alt="在这里插入图片描述"></p>
<p><strong>视图的作用：</strong></p>
<ol>
<li>视图能简化用户操作</li>
<li>视图是用户能以多种角度看待同一数据</li>
<li>视图对重构数据库提供了一定程度的逻辑独立性</li>
<li>视图能够对机密数据提供安全保护</li>
<li>适当利用视图可以更清晰地表达查询</li>
</ol>
<h2 id="第五章"><a href="#第五章" class="headerlink" title="第五章"></a>第五章</h2><h2 id="1、什么是存储过程？使用存储过程的优点？如何创建和调用存储过程？"><a href="#1、什么是存储过程？使用存储过程的优点？如何创建和调用存储过程？" class="headerlink" title="1、什么是存储过程？使用存储过程的优点？如何创建和调用存储过程？"></a>1、什么是存储过程？使用存储过程的优点？如何创建和调用存储过程？</h2><h3 id="1、存储过程："><a href="#1、存储过程：" class="headerlink" title="1、存储过程："></a><strong>1、存储过程：</strong></h3><p>存储过程是由过程化SQL语句书写的过程，这个过程经编译和优化后存储在数据服务器中，因此称它为存储过程，使用时只需要调用即可。<br><strong>2、存储过程的优点<img src="https://raw.githubusercontent.com/leoz9/picture/main/blog/202304081430993.png" alt="3d5e27057ee44fa7879af7be56a794c5"></strong></p>
<h4 id="2、什么是触发器？触发器的作用？如何定义一个触发器？"><a href="#2、什么是触发器？触发器的作用？如何定义一个触发器？" class="headerlink" title="2、什么是触发器？触发器的作用？如何定义一个触发器？"></a>2、什么是触发器？触发器的作用？如何定义一个触发器？</h4><p>1、触发器的定义：<br>触发器是与表有关的数据库对象，在满足定义条件时触发，并执行触发器中定义的语句集合，可以用来在数据库端确保数据的完整性。</p>
<p>2、触发器的作用：<br>触发器的主要作用就是其能够实现由主键和外键所不能保证的复杂的参照完整性和数据的一致性。</p>
<h2 id="第六章-数据库设计"><a href="#第六章-数据库设计" class="headerlink" title="第六章 数据库设计"></a>第六章 数据库设计</h2><ol>
<li>需求分析</li>
<li>概念结构设计</li>
<li>逻辑结构设计</li>
<li>物理结构设计</li>
<li>数据库实施</li>
<li>数据库运行和维护</li>
</ol>
<h4 id="实体联系模型的要素有哪些？"><a href="#实体联系模型的要素有哪些？" class="headerlink" title="实体联系模型的要素有哪些？"></a>实体联系模型的要素有哪些？</h4><p>1、实体(Entity)：<br>具有相同属性的实体具有相同的特征和性质，用实体名及其属性名集合来抽象和刻画同类实体;<br>在E-R图中用矩形表示，矩形框内写明实体名；<br>比如学生张三丰、学生李寻欢都是实体。如果是弱实体的话，在矩形外面再套实线矩形。<br>2、属性(Attribute)：<br>实体所具有的某一特性，一个实体可由若干个属性来刻画。<br>在E-R图中用椭圆形表示，并用无向边将其与相应的实体连接起来；比如学生的姓名、学号、性别、都是属性。如果是多值属性的话，再椭圆形外面再套实线椭圆。如果是派生属性则用虚线椭圆表示。<br>3、联系(Relationship)：<br>数据对象彼此之间相互连接的方式称为联系，也称为关系。联系可分为以下 3 种类型：<br>(1) 一对一联系 (1 ∶ 1)<br>例如，一个部门有一个经理，而每个经理只在一个部门任职，则部门与经理的联系是一对一的。<br>(2) 一对多联系 (1 ∶ N)<br>例如，某校教师与课程之间存在一对多的联系“教”，即每位教师可以教多门课程，但是每门课程只能由一位教师来教。<br>(3) 多对多联系 (M ∶ N)<br>例如，学生与课程间的联系(“ 学 ”)是多对多的，即一个学生可以学多门课程，而每门课程可以有多个学生来学。联系也可能有属性。例如，学生 “ 学 ” 某门课程所取得的成绩，既不是学生的属性也不是课程的属性。由于 “ 成绩 ” 既依赖于某名特定的学生又依赖于某门特定的课程，所以它是学生与课程之间的联系 “ 学 ”的属性.</p>
<h2 id="第七章-关系数据库理论"><a href="#第七章-关系数据库理论" class="headerlink" title="第七章 关系数据库理论"></a>第七章 关系数据库理论</h2><h3 id="1、什么是函数依赖、完全部分依赖、直接传递依赖？"><a href="#1、什么是函数依赖、完全部分依赖、直接传递依赖？" class="headerlink" title="1、什么是函数依赖、完全部分依赖、直接传递依赖？"></a>1、什么是函数依赖、完全部分依赖、直接传递依赖？</h3><p><img src="https://raw.githubusercontent.com/leoz9/picture/main/blog/202304081432824.png" alt="f5c40909a1d2428e985a7602322b9873"></p>
<h3 id="2、什么是范式，什么是规范化？"><a href="#2、什么是范式，什么是规范化？" class="headerlink" title="2、什么是范式，什么是规范化？"></a>2、什么是范式，什么是规范化？</h3><p><strong>（1）范式：</strong><br>关系数据库中的关系是要满足一定要求的，满足不同程度要求的为不同范式<br><strong>（2）规范化：</strong><br>一个低一级范式的关系模型通过模式分解（schema decomposition）可以转换为若干个高一级范式的关系模式的集合这种过程叫做规范化。<img src="https://raw.githubusercontent.com/leoz9/picture/main/blog/202304081433175.png" alt="35f93ea34f814636873761fa24efa82c"></p>
<ol>
<li>有部分依赖就不是2NF ②是2NF，但是有传递依赖就不是3NF</li>
</ol>
<p><a href="https://www.cnblogs.com/linjiqin/archive/2012/04/01/2428695.html">https://www.cnblogs.com/linjiqin/archive/2012/04/01/2428695.html</a></p>
]]></content>
      <tags>
        <tag>数据库</tag>
      </tags>
  </entry>
  <entry>
    <title>机器学习中常见的几种梯度下降法</title>
    <url>/posts/f65f790f/</url>
    <content><![CDATA[<h1 id="批量梯度下降-BGD-、随机梯度下降-SGD-、小批量梯度下降-MBGD"><a href="#批量梯度下降-BGD-、随机梯度下降-SGD-、小批量梯度下降-MBGD" class="headerlink" title="批量梯度下降(BGD)、随机梯度下降(SGD)、小批量梯度下降(MBGD)"></a>批量梯度下降(BGD)、随机梯度下降(SGD)、小批量梯度下降(MBGD)</h1><p>梯度下降法作为机器学习中较常使用的优化算法，其有着3种不同的形式：批量梯度下降（Batch Gradient Descent）、随机梯度下降（Stochastic Gradient Descent）、小批量梯度下降（Mini-Batch Gradient Descent）。其中小批量梯度下降法也常用在深度学习中进行模型的训练。接下来，我们将对这3种不同的梯度下降法进行理解。</p>
<span id="more"></span>
<h2 id="1-批量梯度下降-Batch-Gradient-Descent，BGD"><a href="#1-批量梯度下降-Batch-Gradient-Descent，BGD" class="headerlink" title="1. 批量梯度下降(Batch Gradient Descent，BGD)"></a>1. 批量梯度下降(Batch Gradient Descent，BGD)</h2><p>使用整个训练集的优化算法被称为批量(batch)或确定性(deterministic)梯度算法，因为它们会在一个大批量中同时处理所有样本。</p>
<p>优点：</p>
<p>（1）在训练过程中，使用固定的学习率，不必担心学习率衰退现象的出现。</p>
<p>（2）由全数据集确定的方向能够更好地代表样本总体，从而更准确地朝向极值所在的方向。当目标函数为凸函数时，一定能收敛到全局最小值，如果目标函数非凸则收敛到局部最小值。</p>
<p>（3）它对梯度的估计是无偏的。样例越多，标准差越低。</p>
<p>（4）一次迭代是对所有样本进行计算，此时利用向量化进行操作，实现了并行。</p>
<p>缺点：</p>
<p>（1）尽管在计算过程中，使用了向量化计算，但是遍历全部样本仍需要大量时间，尤其是当数据集很大时（几百万甚至上亿），就有点力不从心了。</p>
<p>（2）每次的更新都是在遍历全部样例之后发生的，这时才会发现一些例子可能是多余的且对参数更新没有太大的作用。</p>
<p>从迭代的次数上来看，BGD迭代的次数相对较少。</p>
<h2 id="2-随机梯度下降-Stochastic-Gradient-Descent，SGD"><a href="#2-随机梯度下降-Stochastic-Gradient-Descent，SGD" class="headerlink" title="2. 随机梯度下降(Stochastic Gradient Descent，SGD)"></a>2. 随机梯度下降(Stochastic Gradient Descent，SGD)</h2><p>优点：</p>
<p>（1）在学习过程中加入了噪声，提高了泛化误差。</p>
<p>（2）由于不是在全部训练数据上的损失函数，而是在每轮迭代中，随机优化某一条训练数据上的损失函数，这样每一轮参数的更新速度大大加快。</p>
<p>缺点：</p>
<p>（1）不收敛，在最小值附近波动。</p>
<p>（2）不能在一个样本中使用向量化计算，学习过程变得很慢。</p>
<p>（3）单个样本并不能代表全体样本的趋势。</p>
<p>（4）当遇到局部极小值或鞍点时，SGD会卡在梯度为0处。</p>
<p>从迭代的次数上来看，SGD迭代的次数较多，在解空间的搜索过程看起来很盲目。</p>
<h2 id="3-小批量梯度下降-Mini-batch-Gradient-Descent，MBGD"><a href="#3-小批量梯度下降-Mini-batch-Gradient-Descent，MBGD" class="headerlink" title="3. 小批量梯度下降(Mini-batch Gradient Descent，MBGD)"></a>3. 小批量梯度下降(Mini-batch Gradient Descent，MBGD)</h2><p>大多数用于深度学习的梯度下降算法介于以上两者之间，使用一个以上而又不是全部的训练样本。传统上，这些会被称为小批量(mini-batch)或小批量随机(mini-batch stochastic)方法，现在通常将它们简单地成为随机(stochastic)方法。对于深度学习模型而言，人们所说的“随机梯度下降, SGD”，其实就是基于小批量（mini-batch）的随机梯度下降。</p>
<p>优点：</p>
<p>（1）计算速度比Batch Gradient Descent快，因为只遍历部分样例就可执行更新。</p>
<p>（2）随机选择样例有利于避免重复多余的样例和对参数更新较少贡献的样例。</p>
<p>（3）每次使用一个batch可以大大减小收敛所需要的迭代次数，同时可以使收敛到的结果更加接近梯度下降的效果。</p>
<p>缺点：</p>
<p>（1）在迭代的过程中，因为噪音的存在，学习过程会出现波动。因此，它在最小值的区域徘徊，不会收敛。</p>
<p>（2）学习过程会有更多的振荡，为更接近最小值，需要增加学习率衰减项，以降低学习率，避免过度振荡。</p>
<p>（3）batch_size的不当选择可能会带来一些问题。</p>
]]></content>
      <categories>
        <category>machine learning</category>
      </categories>
      <tags>
        <tag>machine learning</tag>
      </tags>
  </entry>
  <entry>
    <title>软件工程</title>
    <url>/posts/7821ac43/</url>
    <content><![CDATA[<h1 id="一、软件与软件工程"><a href="#一、软件与软件工程" class="headerlink" title="一、软件与软件工程"></a>一、软件与软件工程</h1><h2 id="软件危机"><a href="#软件危机" class="headerlink" title="软件危机"></a>软件危机</h2><h3 id="1-Software-x3D-Program-Data-Document"><a href="#1-Software-x3D-Program-Data-Document" class="headerlink" title="1.Software&#x3D;Program+Data+Document"></a>1.Software&#x3D;Program+Data+Document</h3><h3 id="2-软件的特点："><a href="#2-软件的特点：" class="headerlink" title="2.软件的特点："></a>2.软件的特点：</h3><p>（1）无形</p>
<p>（2）副本制作简单</p>
<p>（3）无磨损</p>
<h3 id="3-软件危机：是指在计算机软件的开发和维护过程中所遇到的一系列严重问题。"><a href="#3-软件危机：是指在计算机软件的开发和维护过程中所遇到的一系列严重问题。" class="headerlink" title="3.软件危机：是指在计算机软件的开发和维护过程中所遇到的一系列严重问题。"></a>3.软件危机：是指在计算机软件的开发和维护过程中所遇到的一系列严重问题。</h3><h3 id="4-软件危机的典型表现："><a href="#4-软件危机的典型表现：" class="headerlink" title="4.软件危机的典型表现："></a>4.软件危机的典型表现：</h3><p>（1）对软件开发成本和进度的估计常常很不准确；</p>
<p>（2）用户对“已完成的”软件系统不满意的现象经常发生；</p>
<p>（3）软件产品的质量往往靠不住；</p>
<p>（4）软件常常是不可维护的；</p>
<p>（5）软件通常没有适合的文档资料；</p>
<p>（6）软件成本在计算机系统总成本中所占的比例逐年上升；</p>
<p>（7）软件开发生产率提高的速度，远远跟不上计算机应用迅速普及深入的趋势。</p>
<h3 id="5-产生软件危机的原因："><a href="#5-产生软件危机的原因：" class="headerlink" title="5.产生软件危机的原因："></a>5.产生软件危机的原因：</h3><p>一方面与软件本身的特点有关，另一方面也和软件开发与维护的方法不正确有关。</p>
<h2 id="二、软件工程"><a href="#二、软件工程" class="headerlink" title="二、软件工程"></a>二、软件工程</h2><p>1.中心思想：<br>把软件当作一种工业产品，要求采用工程化的原理与方法对软件进行计划、开发和维护。</p>
<p>2.目的：<br>实现按预期的进度和经费完成软件生产计划，提高软件的生产率和可靠性。</p>
<p>3.基本流程：<br>（1）首先进行系统调查和系统分析;<br>（2）再进行逻辑设计、物理设计;<br>（3）最后才编制实施；<br>（4）反复测试;<br>（5）试运行后，再投入长期运行;<br>（6）在运行中不断维护、完善。</p>
<p>三、软件生命周期</p>
<img src="https://raw.githubusercontent.com/leoz9/picture/main/blog/202304081330387.png" style="zoom:50%;" />

<h3 id="四、软件工程模型"><a href="#四、软件工程模型" class="headerlink" title="四、软件工程模型"></a>四、软件工程模型</h3><h4 id="1-瀑布模型："><a href="#1-瀑布模型：" class="headerlink" title="1.瀑布模型："></a>1.瀑布模型：</h4><p>（1）阶段具有顺序性和依赖性{①必须等前一阶段的工作完成之后，才能开始后一阶段的工作；②前一阶段的输出文档就是后一阶段的输入文档，因此，只有前一阶段的输入文档正确，后一阶段的工作才能获得正确的结果}</p>
<p>（2）推迟实现的观点</p>
<p>（3）质量保证的观点{①每个阶段都必须完成规定的文档；②每个阶段结束前都要对所完成的文档进行评审}</p>
<p>瀑布模型基本上是一种文档驱动的模型<br>优点： 可强迫开发人员采用规范的方法；严格规定了每个阶段必须提交的文档；每个阶段交出的所有产品都必须经过质量保证小组的仔细验证。<br>缺点： 用户只能通过文档了解产品，很可能导致最终开发出的软件产品不能真正满足用户的需要。</p>
<h4 id="2-快速原型模型："><a href="#2-快速原型模型：" class="headerlink" title="2.快速原型模型："></a>2.快速原型模型：</h4><p>快速原型的本质是“快速”，快速原型模型是不带反馈环的<br>优点： 有助于保证用户的真实需要的得到满足；软件产品的开发基本上是线性顺序进行的。<br>缺点： 系统结构通常较差；用户可能混淆原型系统和最终系统。</p>
<h4 id="3-增量模型："><a href="#3-增量模型：" class="headerlink" title="3.增量模型："></a>3.增量模型：</h4><p>增量模型也称为渐增模型。使用增量模型开发软件时，把软件产品作为一系列的增量构件来设计、编码、集成和测试。使用增量模型时，第一个增量构件往往实现软件的基本需求，提供最核心的功能。<br>优点： 能在较短时间内向用户提交可完成部分工作的产品；逐步增加产品功能可以使用户有较充裕的时间学习和适应新产品。<br>难点： 软件体系结构必须是开放的；不同的构件可能无法集成到一体。</p>
<h4 id="4-螺旋模型："><a href="#4-螺旋模型：" class="headerlink" title="4.螺旋模型："></a>4.螺旋模型：</h4><p>螺旋模型的基本思想是：使用原型及其他方法来尽量降低风险。<br>理解这种模型的一个简便方法，是把它看作在每个阶段之前都增加了风险分析过程的快速原型模型。适用于内部开发的大规模软件项目。<br>优点： （风险驱动）对可选方案和约束条件的强调有利于已有软件的重用，也有助于把软件质量作为软件开发的一个重要目标；减少了过多测试和测试不足所带来的风险；维护和开发之间并没有本质区别。<br>缺点： 过多的迭代次数可能会增加成本，延迟提交时间。</p>
<h4 id="5-喷泉模型："><a href="#5-喷泉模型：" class="headerlink" title="5.喷泉模型："></a>5.喷泉模型：</h4><p>“喷泉”这个词体现了面向对象软件开发过程迭代和无缝的特性。<br>喷泉模型适用于面向对象的软件开发<br>优点： 各个阶段没有明确的界限，开发人员可以同步开发；降低错误的可能性。<br>缺点： 需要大量的开发人员，不利于项目的管理；要求严格管理文档，审核难度大。</p>
<h1 id="第二章-可行性研究"><a href="#第二章-可行性研究" class="headerlink" title="第二章 可行性研究"></a>第二章 可行性研究</h1><h2 id="一、可行性研究"><a href="#一、可行性研究" class="headerlink" title="一、可行性研究"></a>一、可行性研究</h2><h4 id="1-可行性研究的目的："><a href="#1-可行性研究的目的：" class="headerlink" title="1.可行性研究的目的："></a>1.可行性研究的目的：</h4><p>不是解决问题，而是确定问题是否值得去解决。</p>
<h4 id="2-可行性研究的实质："><a href="#2-可行性研究的实质：" class="headerlink" title="2.可行性研究的实质："></a>2.可行性研究的实质：</h4><p>进行一次大大压缩简化了的系统分析和设计的过程，也就是在较高层次上以较抽象的方式进行的系统分析和设计的过程。</p>
<h4 id="3-可行性研究的内容："><a href="#3-可行性研究的内容：" class="headerlink" title="3.可行性研究的内容："></a>3.可行性研究的内容：</h4><p>（1）首先进一步分析和澄清问题定义，导出系统的逻辑模型；</p>
<p>（2）然后从系统逻辑模型出发，探索若干种可供选择的主要解法(即系统实现方案)；</p>
<p>（3）对每种解法都研究它的可行性，至少应该从三方面研究每种解法的可行性 。</p>
<h4 id="4-可行性研究的方面："><a href="#4-可行性研究的方面：" class="headerlink" title="4.可行性研究的方面："></a>4.可行性研究的方面：</h4><p>（1）主要方面：技术可行性、经济可行性、操作可行性</p>
<p>（2）其他方面：法律可行性、运行可能性</p>
<h4 id="5-可行性研究过程："><a href="#5-可行性研究过程：" class="headerlink" title="5.可行性研究过程："></a>5.可行性研究过程：</h4><p>（1）复查系统规模和目标</p>
<p>（2）研究目前正在使用的系统</p>
<p>（3）导出新系统的高层逻辑模型</p>
<p>（4）进一步定义问题</p>
<p>（5）导出和评价供选择的解法</p>
<p>（6）推荐行动方针</p>
<p>（7）草拟开发计划</p>
<p>（8）书写文档提交审查</p>
<h2 id="二、成本-x2F-效益分析"><a href="#二、成本-x2F-效益分析" class="headerlink" title="二、成本&#x2F;效益分析"></a>二、成本&#x2F;效益分析</h2><p>例：假设某软件生命周期为5年。现在投资20万元，平均年利率3%。从第一年起，每年年底收入4.2万元，问该项目是否值得投资？</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">n年后收入F万元，这些钱现在的价值是：P=F/(1+i)^n^</span><br><span class="line">（1）纯收入</span><br><span class="line">4.2[1/(1+3%)^1+1/(1+3%)^2+1/(1+3%)^3+1/(1+3%)^4+1/(1+3%)^5]-20=-0.7652（万元）</span><br><span class="line">（2）投资回收期</span><br><span class="line">4.2/(1+3%)^6=3.5174（万元）</span><br><span class="line">0.7652/3.5174=0.2175（年）</span><br><span class="line">5+0.2175=5.2175（年）</span><br></pre></td></tr></table></figure>

<h2 id="三、数据流图（DFD）"><a href="#三、数据流图（DFD）" class="headerlink" title="三、数据流图（DFD）"></a>三、数据流图（DFD）</h2><h3 id="1-数据流图的概念："><a href="#1-数据流图的概念：" class="headerlink" title="1.数据流图的概念："></a>1.数据流图的概念：</h3><p>数据流图（DFD）是一种图形化技术，它描绘信息流和数据从输入移动到输出的过程中所经受的变换。在数据流图中没有任何具体的物理部件，它只是描绘数据在软件中流动和被处理的逻辑过程。</p>
<h3 id="2-画数据流图的步骤："><a href="#2-画数据流图的步骤：" class="headerlink" title="2.画数据流图的步骤："></a>2.画数据流图的步骤：</h3><p>（1）从问题描述中提取数据流图的4种成分</p>
<p>（2）画基本系统模型</p>
<p>（3）细化，描绘系统的主要功能（功能级数据流图）</p>
<p>（4）对系统主要功能进一步细化</p>
<h2 id="四、数据字典"><a href="#四、数据字典" class="headerlink" title="四、数据字典"></a>四、数据字典</h2><h3 id="1-数据字典的概念："><a href="#1-数据字典的概念：" class="headerlink" title="1.数据字典的概念："></a>1.数据字典的概念：</h3><p>数据字典是关于数据的信息的集合，也就是对数据流图中包含的所有元素的定义的集合。</p>
<h3 id="2-数据字典的内容："><a href="#2-数据字典的内容：" class="headerlink" title="2.数据字典的内容："></a>2.数据字典的内容：</h3><p>（1）数据流；</p>
<p>（2）数据流分量（即数据元素）；</p>
<p>（3）数据存储；</p>
<p>（4）处理。</p>
<h3 id="3-定义数据的方法："><a href="#3-定义数据的方法：" class="headerlink" title="3.定义数据的方法："></a>3.定义数据的方法：</h3><p><strong>方法：</strong> 对数据自顶向下分解。</p>
<h1 id="第三章-需求分析"><a href="#第三章-需求分析" class="headerlink" title="第三章 需求分析"></a>第三章 需求分析</h1><h2 id="一、需求分析"><a href="#一、需求分析" class="headerlink" title="一、需求分析"></a>一、需求分析</h2><p>1.需求分析的概念与任务：<br>需求分析是软件定义时期的最后一个阶段，它的基本任务是准确地回答“系统必须做什么”这个问题。</p>
<h2 id="二、分析建模与规格说明"><a href="#二、分析建模与规格说明" class="headerlink" title="二、分析建模与规格说明"></a>二、分析建模与规格说明</h2><p>需求分析应该建立三种模型：数据模型、功能模型、行为模型<img src="https://raw.githubusercontent.com/leoz9/picture/main/blog/202304081357154.png" alt="截屏2023-04-08 13.5" style="zoom:50%;" /></p>
<p>（1）数据字典：是分析模型的核心，它描述软件使用或产生的所有数据对象。</p>
<p>（2）实体-联系图：描绘数据对象及数据对象之间的关系，是用于建立数据模型的图形。</p>
<p>（3）数据流图：描绘当数据在软件系统中移动时被变换的逻辑过程，指明系统具有的变换数据的功能，因此，数据流图是建立功能模型的基础。</p>
<p>（4）状态转换图(简称为状态图)：指明了作为外部事件结果的系统行为。为此，状态转换图描绘了系统的各种行为模式(称为“状态”)和在不同状态间转换的方式。状态转换图是行为建模的基础。</p>
<h2 id="三、实体-联系图（ER图）"><a href="#三、实体-联系图（ER图）" class="headerlink" title="三、实体-联系图（ER图）"></a>三、实体-联系图（ER图）</h2><h3 id="1-数据对象："><a href="#1-数据对象：" class="headerlink" title="1.数据对象："></a>1.数据对象：</h3><p>是对软件必须理解的复合信息的抽象。数据对象可以是外部实体、事物、行为、事件、角色、单位、地点或结构等。数据对象彼此间是有关联的。</p>
<h3 id="2-属性："><a href="#2-属性：" class="headerlink" title="2.属性："></a>2.属性：</h3><p>定义了数据对象的性质。</p>
<h3 id="3-联系："><a href="#3-联系：" class="headerlink" title="3.联系："></a>3.联系：</h3><p>数据对象彼此之间相互连接的方式称为联系，也称为关系。<br>联系可分为以下3种类型：<br>（1）一对一联系(1∶1)<br>（2）一对多联系(1∶N)<br>（3）多对多联系(M∶N)<br>联系也可能有属性。</p>
<h1 id="第四章-总体设计"><a href="#第四章-总体设计" class="headerlink" title="第四章 总体设计"></a>第四章 总体设计</h1><p>总体设计的基本目的就是回答“概括地说，系统应该如何实现”这个问题，因此，总体设计又称为概要设计或初步设计。</p>
<h2 id="一、设计过程"><a href="#一、设计过程" class="headerlink" title="一、设计过程"></a>一、设计过程</h2><h3 id="1-总体设计过程通常由两个主要阶段组成："><a href="#1-总体设计过程通常由两个主要阶段组成：" class="headerlink" title="1.总体设计过程通常由两个主要阶段组成："></a>1.总体设计过程通常由两个主要阶段组成：</h3><p>（1）系统设计阶段：确定系统的具体实现方案<br>（2）结构设计阶段：确定软件的结构</p>
<h2 id="2-典型的总体设计过程包括9个步骤："><a href="#2-典型的总体设计过程包括9个步骤：" class="headerlink" title="2.典型的总体设计过程包括9个步骤："></a>2.典型的总体设计过程包括9个步骤：</h2><p>（1）设想供选择的方案；<br>（2）选取合理的方案；<br>（3）推荐最佳方案；<br>（4）功能分解；<br>（5）设计软件结构；<br>（6）设计数据库；<br>（7）制定测试计划；<br>（8）书写文档；<br>（9）审查和复查。</p>
<h2 id="二、设计原理"><a href="#二、设计原理" class="headerlink" title="二、设计原理"></a>二、设计原理</h2><p>1.模块化<br>（1）模块化的定义：<br>就是把程序划分成独立命名且可独立访问的模块，每个模块完成一个子功能，把这些模块集成起来构成一个整体，可以完成指定的功能满足用户的需求。<br>（2）为什么要模块化：</p>
<p>模块化是为了使一个复杂的大型程序能被人的智力所管理，软件应该具备的惟一属性。<br>如果一个大型程序仅由一个模块组成，它将很难被人所理解。<br>2.抽象<br>（1）抽象的定义：<br>现实世界中一定事物、状态或过程之间总存在着某些相似的方面(共性)。把这些相似的方面集中和概括起来，暂时忽略它们之间的差异，这就是抽象。抽象就是抽出事物本质特性而暂时不考虑细节。</p>
<p>3.逐步求精<br>（1）逐步求精定义：<br>为了能集中精力解决主要问题而尽量推迟对问题细节的考虑。逐步求精是人类解决复杂问题时采用的基本方法，也是许多软件工程技术的基础。<br>（2）Miller法则：<br>一个人在任何时候都只能把注意力集中在（7±2）个知识块上。</p>
<p>4.信息隐藏和局部化<br>（1）信息隐藏：<br>应该这样设计和确定模块，使得一个模块内包含的信息(过程和数据)对于不需要这些信息的模块来说，是不能访问的。<br>（2）局部化：<br>局部化的概念和信息隐藏概念是密切相关的。所谓局部化是指把一些关系密切的软件元素物理地放得彼此靠近。显然，局部化有助于实现信息隐藏。</p>
<p>5.模块独立<br>（1）模块独立的概念是模块化、抽象、信息隐藏和局部化概念的直接结果。 希望这样设计软件结构，使得每个模块完成一个相对独立的特定子功能，并且和其他模块之间的关系很简单。<br>（2）模块独立程度的两个定性标准度量：</p>
<p>耦合：对一个软件结构内不同模块之间互连程度的度量。<br>①非直接耦合&#x2F;完全独立：两个模块每一个都能独立地工作而不需要零一个模块的存在。(不存在)<br>②数据耦合：两个模块通过参数交换信息，而且交换的信息仅仅是数据。<br>③控制耦合：传递的信息中有控制信息。<br>④特征耦合：把整个数据结构作为参数传递而被调用的模块只需要使用其中一部分数据元素。<br>⑤公共环境耦合：两个或多个模块通过一个公共数据环境相互作用。<br>⑥内容耦合：一个模块访问另一个模块的内部数据；一个模块不通过正常入口而转到另一个模块内部；两个模块有一部分程序代码重叠；一个模块有多个入口。<br>耦合程度：内容耦合&gt;公共环境耦合(两个模块既送又取)&gt;控制耦合&gt;数据耦合&gt;完全独立<br>设计要求：尽量使用数据耦合，少用控制耦合和特征耦合，限制公共环境耦合的范围，完全不用内容耦合。</p>
<p>内聚：一个模块内各个元素彼此结合的紧密程度。<br>①偶然内聚：一个模块完成一组任务，任务之间即使有关系，关系也是很松散的。<br>②逻辑内聚：一个模块完成的任务在逻辑上属于相同或相似的一类。<br>③时间内聚：一个模块包含的任务必须在同一段时间内执行。<br>④过程内聚：一个模块内的处理元素是相关的，而且必须以特定次序执行。<br>⑤通信内聚：模块中所有元素都使用同一个输入数据和产生同一个输出数据。<br>⑥顺序内聚：一个模块内的处理元素和同一个功能密切相关，而且处理必须顺序执行。<br>⑦功能内聚：模块内所有处理元素属于一个整体，完成一个单一的功能。<br>低内聚：偶然内聚、逻辑内聚、时间内聚；<br>中内聚：过程内聚、通信内聚；<br>高内聚：顺序内聚、功能内聚。<br>设计要求：力争做到高内聚，并且能够辨认出低内聚的模块。</p>
<h2 id="三、启发规则"><a href="#三、启发规则" class="headerlink" title="三、启发规则"></a>三、启发规则</h2><p>1.改进软件结构提高模块独立性：<br>通过模块分解或合并，降低耦合提高内聚</p>
<p>2.模块规模应该适中<br>3.深度、宽度、扇入和扇出都应适当：<br>（1）深度：软件结构中控制的层数，它往往能粗略地标志一个系统的大小和复杂程度。<br>（2）宽度：软件结构内同一个层次上的模块总数的最大值。<br>（3）扇出：一个模块直接控制(调用)的模块数目。<br>（4）扇入：有多少个上级模块直接调用它。</p>
<h1 id="第五章-详细设计"><a href="#第五章-详细设计" class="headerlink" title="第五章 详细设计"></a>第五章 详细设计</h1><h2 id="一、结构程序设计"><a href="#一、结构程序设计" class="headerlink" title="一、结构程序设计"></a>一、结构程序设计</h2><h3 id="1-经典定义："><a href="#1-经典定义：" class="headerlink" title="1.经典定义："></a>1.经典定义：</h3><p>如果一个程序的代码块仅仅通过顺序、选择和循环这3种基本控制结构进行连接，并且每个代码块只有一个入口和一个出口，则称这个程序是结构化的。</p>
<h3 id="2-更全面的定义："><a href="#2-更全面的定义：" class="headerlink" title="2.更全面的定义："></a>2.更全面的定义：</h3><p>结构程序设计是尽可能少用GOTO语句的程序设计方法。最好仅在检测出错误时才使用GOTO语句，而且应该总是使用前向GOTO语句。</p>
<h2 id="二、人机界面设计"><a href="#二、人机界面设计" class="headerlink" title="二、人机界面设计"></a>二、人机界面设计</h2><h3 id="1-三条黄金规则："><a href="#1-三条黄金规则：" class="headerlink" title="1.三条黄金规则："></a>1.三条黄金规则：</h3><p>（1）置用户于控制之下；<br>（2）减少用户记忆负担；<br>（3）保持界面一致。</p>
<h3 id="2-设计问题："><a href="#2-设计问题：" class="headerlink" title="2.设计问题："></a>2.设计问题：</h3><p>（1）系统相应时间<br>（2）用户帮助设施<br>（3）出错信息处理<br>（4）命令交互</p>
<h2 id="三、过程设计的工具"><a href="#三、过程设计的工具" class="headerlink" title="三、过程设计的工具"></a>三、过程设计的工具</h2><p>1.程序流程图<br>2.盒图（N-S图）<br>3.PAD图<br>4.判定表<br>5.判定树<br>6.过程设计语言（PDL或伪码）</p>
<h1 id="软件测试"><a href="#软件测试" class="headerlink" title="软件测试"></a>软件测试</h1><h2 id="一、软件测试"><a href="#一、软件测试" class="headerlink" title="一、软件测试"></a>一、软件测试</h2><p>1.（软件）测试的定义或目标<br>（1）测试是为了发现程序中的错误而执行程序的过程<br>（2）好的测试方案极可能是发现了迄今为止尚未发现的错误的测试方案<br>（3）成功的测试是发现了至今为止尚未发现的错误的测试</p>
<p>2.测试方法<br>（1）黑盒测试（功能测试）<br>把程序看作一个黑盒子；完全不考虑程序的内部结构和处理过程；是在程序接口进行的测试<br>（2）白盒测试（结构测试）<br>把程序看成装在一个透明的白色盒子里；测试者完全知道程序的结构和处理算法；按照程序内部的逻辑测试程序，检测程序中的主要执行通路是否能按预定要求工作</p>
<p>3.测试步骤<br>模块测试（单元测试）、子系统测试、系统测试、验收测试、平行运行</p>
<h2 id="二、白盒测试技术"><a href="#二、白盒测试技术" class="headerlink" title="二、白盒测试技术"></a>二、白盒测试技术</h2><p>1.测试用例<br>测试用例&#x3D;测试数据+预期的结果</p>
<p>2.逻辑覆盖<br>（1）语句覆盖<br>每个语句至少执行一次<br>（2）判定覆盖<br>每个语句至少执行一次，每个判定的每个分支至少执行一次<br>（3）条件覆盖<br>每个语句至少执行一次，判定表达式的每种结果至少执行一次<br>条件覆盖通常比判定覆盖强，有时条件覆盖也是判定覆盖<br>（4）判定&#x2F;条件覆盖<br>覆盖判定+条件判定<br>有时判定&#x2F;条件覆盖也不一定比条件覆盖强</p>
<h2 id="三、黑盒测试技术"><a href="#三、黑盒测试技术" class="headerlink" title="三、黑盒测试技术"></a>三、黑盒测试技术</h2><p>1.基本概念<br>（1）黑盒测试着重测试软件功能<br>（2）黑盒测试不能取代白盒测试，两者为互补关系<br>（3）白盒测试—测试过程的早期；黑盒测试——测试过程的后期</p>
<p>2.黑盒测试准则<br>（1）测试用例尽可能少<br>（2）一个测试用例能指出一类错误</p>
<p>3.黑盒测试手段<br>等价划分、边界值分析、错误推测</p>
<h2 id="四、单元测试"><a href="#四、单元测试" class="headerlink" title="四、单元测试"></a>四、单元测试</h2><p>1.单元测试集中检测模块<br>2.单元测试和编码属于软件过程的同一个阶段<br>3.可以应用人工测试和计算机测试这样两种不同类型的测试方法<br>4.单元测试主要使用白盒测试技术，对多个模块的测试可以并行地进行</p>
<h2 id="五、集成测试"><a href="#五、集成测试" class="headerlink" title="五、集成测试"></a>五、集成测试</h2><p>1.集成测试是测试和组装软件的系统化技术，主要目标是发现与接口有关的问题<br>2.集成测试的策略<br>（1）非渐增式<br>（2）自顶向下<br>（3）自底向上<br>（4）混合<br>3.回归测试：重新执行已经做过的测试的某个子集，保证程序的变化没有带来非预期的副作用</p>
<h2 id="六、确认测试"><a href="#六、确认测试" class="headerlink" title="六、确认测试"></a>六、确认测试</h2><p>1.确认测试也称为验收测试，目标是验证软件的有效性<br>2.软件有效性的定义<br>如果软件的功能和性能如用户所合理期待，需求分析阶段产生的软件规格说明书，准确地描述了用户对软件的合理期望，需求分析阶段的软件规格说明书是软件有效性的标准<br>3.Alpha测试和Beta测试</p>
<h2 id="七、调试"><a href="#七、调试" class="headerlink" title="七、调试"></a>七、调试</h2><p>1.调试是在测试发现错误之后排除错误的过程<br>2.调试的方法<br>蛮干法、回溯法、原因排除法</p>
<h2 id="八、软件可靠性"><a href="#八、软件可靠性" class="headerlink" title="八、软件可靠性"></a>八、软件可靠性</h2><p>1.软件可靠性<br>程序在给定的时间间隔内，按照规格说明书的规定成功地运行的概率<br>2.估算平均无故障时间的方法<br>（1）计算平均无故障时间&#x2F;需要改正多少个错误测试结束</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><h2 id="1-软件工程主要分为两种研究方法："><a href="#1-软件工程主要分为两种研究方法：" class="headerlink" title="1.软件工程主要分为两种研究方法："></a>1.软件工程主要分为两种研究方法：</h2><p>结构化的研究方法和面向对象的研究方法</p>
<p>结构化的设计方法</p>
<p>软件工程与软件危机</p>
<h2 id="2-软件危机"><a href="#2-软件危机" class="headerlink" title="2.软件危机"></a>2.软件危机</h2><p>定义：软件开发和维护过程中遇到的一系列严重问题</p>
<p>软件危机产生原因：与软件本身的特性有关（无形、副本制作简单、无磨损）、与软件开发和维护的方法不正确有关</p>
<h2 id="3-软件生命周期（考点）"><a href="#3-软件生命周期（考点）" class="headerlink" title="3.软件生命周期（考点）"></a>3.软件生命周期（考点）</h2><p>软件生命周期可以分为——三个时期八个阶段<br>三个时期：软件定义、软件开发、软件维护<br>八个阶段：问题定义、可行性研究、需求分析；概要（总体）设计、详细设计、编码和单元测试、综合测试；运行维护。<br>其中，各阶段的目的（任务）：<br>（1）可行性研究：不是解决问题，而是要知道问题是否值得被解决<br>（2）需求分析：明确地知道系统必须要做什么<br>（3）概要（总体）设计：系统应该如何实现<br>（4）详细设计：逻辑上正确地实现每个模块的功能，设计的处理过程尽可能简明易懂</p>
<h3 id="4-软件工程模型（考点）"><a href="#4-软件工程模型（考点）" class="headerlink" title="4.软件工程模型（考点）"></a>4.软件工程模型（考点）</h3><p>瀑布模型（线性顺序、每个阶段需求明确）、快速原型模型（无反馈环、满足用户需求）、增量模型（充裕的时间学习和适应）、螺旋模型（风险驱动、迭代）、喷泉模型（面向对象、迭代）</p>
<p>可行性研究</p>
<h3 id="5-可行性研究的方面（考点）"><a href="#5-可行性研究的方面（考点）" class="headerlink" title="5.可行性研究的方面（考点）"></a>5.可行性研究的方面（考点）</h3><p>技术可行性、经济可行性、操作可行性、（法律可行性、运行可行性）</p>
<h3 id="6-可行性研究的过程（考点）"><a href="#6-可行性研究的过程（考点）" class="headerlink" title="6.可行性研究的过程（考点）"></a>6.可行性研究的过程（考点）</h3><p>（1）复查系统规模和目标<br>（2）研究当前正在使用的系统<br>（3）导出新系统高层逻辑模型<br>（4）重新定义问题<br>（5）导出和评价供选择的方案<br>（6）推荐行动方针<br>（7）草拟开发计划<br>（8）书写文档提交审查</p>
<h3 id="7-数据流图（DFD）"><a href="#7-数据流图（DFD）" class="headerlink" title="7.数据流图（DFD）"></a>7.数据流图（DFD）</h3><p>一种图形化技术，描绘信息流和数据从输入移动到输出的过程中所经受的变换</p>
<h3 id="8-数据字典的概念"><a href="#8-数据字典的概念" class="headerlink" title="8.数据字典的概念"></a>8.数据字典的概念</h3><p>关于数据信息的集合，对数据流图中包含所有元素定义的集合<br>数据字典的内容：数据流、数据元素、数据存储、处理<br>数据字典的用途：作为分析阶段的工具</p>
<p>需求分析</p>
<h3 id="9-分析建模"><a href="#9-分析建模" class="headerlink" title="9.分析建模"></a>9.分析建模</h3><p>（1）三种模型：数据模型、功能模型、行为模型<br>（2）三种模型对应：实体-关系图、数据流图、状态图<br>（3）数据字典是分析模型的核心</p>
<h3 id="10-实体-联系图（ER图）"><a href="#10-实体-联系图（ER图）" class="headerlink" title="10.实体-联系图（ER图）"></a>10.实体-联系图（ER图）</h3><p>数据对象、属性、联系（1:1、1:n、m:n）</p>
<p>总体设计</p>
<h3 id="11-总体设计"><a href="#11-总体设计" class="headerlink" title="11.总体设计"></a>11.总体设计</h3><p>主要分为两个阶段</p>
<p>系统设计阶段（确定系统具体实现方案）和结构设计阶段（确定软件结构）</p>
<h3 id="12-耦合"><a href="#12-耦合" class="headerlink" title="12.耦合"></a>12.耦合</h3><p>(强-&gt;弱)内容耦合、公共环境耦合、控制耦合、数据耦合<br>设计要求：尽量使用数据耦合、少用控制耦合、控制公共环境耦合的范围、完全不用内容耦合</p>
<h3 id="13-内聚"><a href="#13-内聚" class="headerlink" title="13.内聚"></a>13.内聚</h3><p>低内聚：偶然内聚、逻辑内聚、时间内聚<br>中内聚：过程内聚、通信内聚<br>高内聚：顺序内聚、功能内聚<br>设计要求：力争做到高内聚</p>
<h3 id="14-描绘软件结构的图形工具："><a href="#14-描绘软件结构的图形工具：" class="headerlink" title="14.描绘软件结构的图形工具："></a>14.描绘软件结构的图形工具：</h3><p>层次图、HIPO图、结构图</p>
<h3 id="15-交换流和事务流的变换分析"><a href="#15-交换流和事务流的变换分析" class="headerlink" title="15.交换流和事务流的变换分析"></a>15.交换流和事务流的变换分析</h3><p>详细设计<br>16.过程设计的工具：程序流程图、盒图（N-S图）、PAD图、判定表、判定树、过程设计语言（PDL或伪码）</p>
<h2 id="面向对象的设计方法"><a href="#面向对象的设计方法" class="headerlink" title="面向对象的设计方法"></a>面向对象的设计方法</h2><p>面向对象方法学<br>17.面向对象方法学的要点：任何事物都是对象；把所有对象分成各种对象类，每个对象类定义一组数据和一组方法；按照子类和父类的关系，可把若干对象类组成一个层次结构系统；对象之间仅能通过传递信息联络</p>
<p>18.面向对象方法学的优点：与人类习惯的思维方式一致；稳定性好；可重用性好；较易开发大型软件产品；维护性好</p>
<p>19.面向对象的概念：<br>对象：具有相同状态的一组操作的集合<br>（对象的特点：以数据为中心、主动、实现数据封装、本质上并行、模块独立）<br>类：对具有相同属性或行为的一个或多个对象的描述<br>实例：由某个特定的类描述一个具体的对象<br>消息：对象操作的规格说明<br>（消息的组成：接收消息的对象、消息选择符、零个或多个变元）<br>方法：对象所能执行的操作<br>属性：类中所定义的数据<br>封装：把数据和实现操作的代码集中起来放在对象内部<br>继承：直接获得已有的性质和特征<br>多态性：子类可以像父类那样使用<br>重载：函数重载：同一个作用域内，参数特征不同的函数可以使用相同的函数名；运算符重载：同一个运算符可以施加于不同类型的操作数上面</p>
<p>20.面向对象建模<br>对象模型：描述系统数据结构<br>（1）描述实体的对象和对象之间的联系，描述了系统的静态结构<br>（2）为动态模型和功能模型提供实质性框架<br>动态模型：描述系统控制结构（状态图）<br>功能模型：描述系统功能（用例图、数据流图）</p>
<p>21.三种模型之间的关系：相互补充、相互配合</p>
<p>22.类与类之间存在的四种关系：关联、继承、依赖、细化</p>
<p>23.类图具有哪些性质：描述类与类之间静止关系；静态模型；一个系统可以由多张类图来描述；一个类也可以出现在几张类图中</p>
<p>24.一幅用例图包含的模型元素：系统、行为者、用例、用例之间关系</p>
<p>25.创建用例模型的步骤（工作）：定义系统、寻找行为者和用例、描述用例、确定用例之间的关系、确定模型</p>
<p>面向对象分析<br>26.面向对象分析的过程：分析陈述用户需求的文件；发现和改正原始陈述的二义性和不一致性，补充遗漏的内容；深入理解用户需求，抽象出目标系统的本质属性，并用模型准确地表达出来（寻找类与对象、识别结构、识别主题、确认属性、建立动态模型、建立功能模型、定义服务）</p>
<p>27.五个层次和五个活动<br>主题层、类与对象层、结构层、属性层、服务层<br>找出类与对象、识别结构、识别主题、定义属性、定义服务</p>
<p>面向对象设计<br>28.面向对象设计的准则：模块化、抽象、信息隐藏、弱耦合、强内聚、可重用</p>
<h2 id="软件测试-1"><a href="#软件测试-1" class="headerlink" title="软件测试"></a>软件测试</h2><p>29.软件测试的目的或定义<br>（1）软件测试是为了发现程序中的错误而执行程序的过程<br>（2）好的测试方案极可能是发现迄今为止尚未发现的错误的测试方案<br>（3）成功的测试是发现至今为止尚未发现的错误的测试</p>
<p>30.软件测试的步骤<br>单元（模块）测试（白盒）、子系统测试（白盒+黑盒）、系统测试（黑盒）、确认（验收）测试（黑盒）、平行运行</p>
<p>31.软件测试用例设计<br>（1）白盒测试技术（前期-结构）<br>测试用例&#x3D;测试数据+预期的结果<br>（考点）逻辑覆盖：语句覆盖、判定覆盖、条件覆盖、判定&#x2F;条件覆盖<br>（2）黑盒测试技术（后期-功能）<br>与白盒测试技术互补<br>（考点）用例尽可能简单、一个测试用例尽可能指出一类问题</p>
<p>32.单元测试与软件编码属于同一个阶段</p>
<p>33.集成测试策略（方法）：非渐增式、自顶向下、自底向上、混合</p>
<p>34.确认（验收）测试检验软件的有效性<br>软件有效性的检验标准：需求分析阶段的软件规格说明书</p>
<p>35.调试：测试发现错误之后排除错误的过程<br>调试的方法：蛮干法、回溯法、原因排除法</p>
<p>36.软件的可靠性（考点）：估计错误总数；完成测试需要改正的错误数</p>
]]></content>
      <tags>
        <tag>软件工程</tag>
      </tags>
  </entry>
  <entry>
    <title>新设置OpenWrt无法正确联网及解决方法</title>
    <url>/posts/eac71423/</url>
    <content><![CDATA[<h1 id="1-问题"><a href="#1-问题" class="headerlink" title="1.问题"></a>1.问题</h1><p>将家里的小米路由器AC2100系统刷为OpenWrt之后，发现无法正确联网，检查各种问题后发现，与移动的光猫的IP地址重合同为192.168.1.1</p>
<h2 id="2-解决方法"><a href="#2-解决方法" class="headerlink" title="2.解决方法"></a>2.解决方法</h2><p>设置静态IP，改为192.168.x.1。</p>
<h2 id="具体操作流程"><a href="#具体操作流程" class="headerlink" title="具体操作流程"></a>具体操作流程</h2><p>1、通过ssh，以root的身份登录系统终端；</p>
<p>2、修改&#x2F;etc&#x2F;config&#x2F;network文件：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">vim /etc/config/network</span><br></pre></td></tr></table></figure>

<p>按i键，把 <code>option gateway &#39;192.168.1.1&#39;</code>改成自己需要的IP地址，如192.168.2.1，再按ESC键，再按：wq保存退出。</p>
<p>执行以上命令，打开网络配置文件，如下：</p>
<img src="https://cdn.leoz.work/img%E6%88%AA%E5%B1%8F2023-02-12%2003.45.46.png" alt="1" style="zoom:50%;" />

<p>3、重启路由器即可。</p>
]]></content>
      <categories>
        <category>openwrt</category>
      </categories>
      <tags>
        <tag>杂谈</tag>
      </tags>
  </entry>
  <entry>
    <title>面向对象程序设计(C++语言)</title>
    <url>/posts/68eaf0fe/</url>
    <content><![CDATA[<h2 id="（1）掌握面向对象程序设计的基本原则"><a href="#（1）掌握面向对象程序设计的基本原则" class="headerlink" title="（1）掌握面向对象程序设计的基本原则"></a>（1）掌握面向对象程序设计的基本原则</h2><p>在软件开发过程中，随着系统复杂度的增加，业务的变更，程序员面临着耦合性，内聚性，可维护性，可扩展性，可重用性的挑战。而良好的设计模式就可以解决此类问题。设计模式是包含了面向对象的精髓，熟练掌握面向对象设计&#x2F;分析是掌握设计模式的基础。可以帮助我们考虑代码结构，使代码易于维护、易于扩展、易于阅读。</p>
<p>那么设计模式本身也是有自己需要遵守的原则，也是各种设计模式的基础。那么设计模式有常用的七大原则。</p>
<span id="more"></span>

<h3 id="1-单一职责（Single-Responsibility-Principle"><a href="#1-单一职责（Single-Responsibility-Principle" class="headerlink" title="1.单一职责（Single Responsibility Principle"></a>1.单一职责（Single Responsibility Principle</h3><p>一个类，只做一件事并且做好。特点：</p>
<p>1）一个类只负责一件事，可以降低类的复杂性。</p>
<p>2）提高类的可读性，可维护性就提高了。</p>
<p>3）降低变更引起可能出现的风险，如果接口的单一职责做得好，一个接口修改只对应的实现类有影响，对其他的接口无影响，这对系统的扩展性、维护性都有非常大的帮助。</p>
<h3 id="2-接口分离原则（Interface-Segregation-Principle）"><a href="#2-接口分离原则（Interface-Segregation-Principle）" class="headerlink" title="2.接口分离原则（Interface Segregation Principle）"></a>2.<strong>接口分离原则（Interface Segregation Principle）</strong></h3><p>为各个类建立他们所需要的专门的接口。把一个接口切分为多个接口，把一个大的职责切分为小职责以及这些职责之间的协作交互。比如一个客户端不应该依赖它不需要的接口，即一个类对另外一个类的依赖应该建立在最小接口上。</p>
<p><strong>1).</strong> 接口要尽量小。</p>
<p>这是接口隔离原则的核心定义，不出现臃肿的接口（Fat Interface），但是“小”是有限度的，首先就是不能违反单一职责原则。<br>　　根据接口隔离原则拆分接口时，首先必须满足单一职责原则。</p>
<p><strong>2).</strong> 接口要高内聚。</p>
<p>高内聚就是要提高接口、类、模块的处理能力，减少对外的交互。<br>　　具体到接口隔离原则就是，要求在接口中尽量少公布public方法，接口是对外的承诺，承诺地越少对系统开发越有利，变更的风险也就越少，同时也有利于降低成本。</p>
<p><strong>3).</strong> 定制服务。</p>
<p>定制服务就是单独为一个个体提供优良的服务。</p>
<p><strong>4).</strong> 接口设计是有限度的。</p>
<p>接口的设计粒度越小，系统越灵活，这是不争的事实。但是，灵活的同时也带来了结构的复杂化，开发难度增加，可维护性降低，这不是一个项目或产品所期望看到的，所以接口设计一定要注意适度，这个度只能根据经验和常识判断，没有一个固化或可测量的标准。</p>
<p>单一职责和接口分离原则的共同点：</p>
<p>1） 接口隔离<em>原则</em>和<em>单一职责</em>都是为了提高类的内聚性、降低它们之间的耦合性，体现了封装的思想。</p>
<p>单一职责和接口分离原则的不同点：</p>
<p>1）单一职责原则注重的是职责，而接口隔离原则注重的是对接口依赖的隔离 </p>
<p>2）单一职责原则针对的是模块、类的设计，注重的是职责，这是业务逻辑上的划分</p>
<p>3）接口隔离原则主要约束接口，主要针对抽象和程序整体框架的构建。</p>
<h3 id="3-开放封闭原则（Open-Close-Principle）"><a href="#3-开放封闭原则（Open-Close-Principle）" class="headerlink" title="3.**开放封闭原则（Open Close **Principle）"></a>3.**开放封闭原则（Open Close **Principle）</h3><p><strong>开放封闭原则是面向对象中最重要的原则，相当于一个总纲领。</strong></p>
<p>一个类应该是对扩展是开放的，对类的内部修改是封闭的，不应该通过修改来增加类的功能，而是通过扩展来增加功能。</p>
<p>比如一个类中有两个功能，现在要增加第三个功能，那么这个新增加的功能不能对另外两个功能有影响。</p>
<h3 id="4-依赖倒置原则（Dependency-Inversion-Principle）"><a href="#4-依赖倒置原则（Dependency-Inversion-Principle）" class="headerlink" title="4.依赖倒置原则（Dependency Inversion Principle）"></a>4.<strong>依赖倒置原则（Dependency Inversion Principle）</strong></h3><p>这条原则说明两个基本的要点：</p>
<p>1）高阶的模块不应该依赖低阶的模块，它们都应该依赖于抽象</p>
<p>2）抽象不应该依赖于实现，实现应该依赖于抽象</p>
<p>一个类不应该强依赖另外一个类，每个类对于另外一个类都是可以替换的。高层模块不需要依赖底层的模块。</p>
<p>比如A类和B类，A类需要B类的相关功能，A类不应该在其内部中直接调用B类，而是应该使用依赖注入的方式通过注入的方式，来将B类的对象注入到A类中，这样B类对于A类来说是可以替换的。高层模块也不应该依赖低层模块，二者都应该依赖其抽象，都是面向接口编程，而不是面向具体实现。接口一般都是稳定的。使用接口或者抽象类可以更好的制定一些规范和契约，而不去涉及具体的操作，具体的实现交给具体的类来完成。</p>
<h3 id="5-里氏替换原则（Liskov-Substitution-Principle）"><a href="#5-里氏替换原则（Liskov-Substitution-Principle）" class="headerlink" title="5.里氏替换原则（Liskov Substitution Principle）"></a>5.里氏替换原则（Liskov Substitution Principle）</h3><p>继承必须确保父类中所拥有的所有性质在子类中依然成立。就是子类可以扩展父类的功能，子类尽量添加新的方法不要改变父类原有的功能，程序中对于实例化对象的子类型，不需要修改代码，可以直接进行替换。</p>
<p>有个著名的说法：正方形不是长方形。</p>
<p>Liskov于1987年提出了一个关于继承的原则 “Inheritance should ensure that any property proved about supertype objects also holds for subtype objects.”（继承必须确保超类所拥有的性质在子类中仍然成立）。</p>
<p>就是说，当一个子类的实例应该能够替换任何其父类的实例时，它们之间才具有is-A关系。</p>
<h3 id="6-合成复用原则（Composite-Reuse-Principle，CRP）"><a href="#6-合成复用原则（Composite-Reuse-Principle，CRP）" class="headerlink" title="6.合成复用原则（Composite Reuse Principle，CRP）"></a>6.合成复用原则（Composite Reuse Principle，CRP）</h3><p>尽量使用组合或者聚合等关联关系来实现，其次在考虑使用继承关系来实现。</p>
<p>组合是Has-A的关系，继承是Is-A关系。“Is-A”是严格的分类学意义上的定义，意思是一个类是另以个类的“一种”。而“Has-A”表示某一个角色具有某一项责任。比如狗和动物是继承关系，狗不能和动物用组合，因为狗不是动物组成的。</p>
<p><strong>合成复用原则和里氏替换原则是相辅相成的，如果要使用继承关系，则必须严格遵循里氏替换原则。</strong></p>
<p><strong>通常类的复用分为继承复用和合成复用两种，继承复用虽然有简单和易实现的优点，但它也存在以下缺点：</strong></p>
<p>1）继承复用破坏了类的封装性。因为继承会将父类的实现细节暴露给子类，父类对子类是透明的，所以这种复用又称为“白箱”复用。</p>
<p>2）子类与父类的耦合度高。父类的实现的任何改变都会导致子类的实现发生变化，这不利于类的扩展与维护。</p>
<p>3）它限制了复用的灵活性。从父类继承而来的实现是静态的，在编译时已经定义，所以在运行时不可能发生变化。</p>
<p>采用组合或聚合复用时，可以将已有对象纳入新对象中，使之成为新对象的一部分，新对象可以调用已有对象的功能，它有以下优点：</p>
<p>1）它维持了类的封装性。因为成分对象的内部细节是新对象看不见的，所以这种复用又称为“黑箱”复用。</p>
<p>2）新旧类之间的耦合度低。这种复用所需的依赖较少，新对象存取成分对象的唯一方法是通过成分对象的接口。</p>
<p>3）复用的灵活性高。这种复用可以在运行时动态进行，新对象可以动态地引用与成分对象类型相同的对象。</p>
<h3 id="7-迪米特法则（Law-of-Demeter）"><a href="#7-迪米特法则（Law-of-Demeter）" class="headerlink" title="7.迪米特法则（Law of Demeter）"></a>7.迪米特法则（Law of Demeter）</h3><p>又称之为最少知识原则（Least Knowledge Principle）。</p>
<p>迪米特法则的定义是：只与你的直接朋友交谈，不跟“陌生人”说话（Talk only to your immediate friends and not to strangers）。其含义是：如果两个类之间无须直接通信，那么就不应当发生直接的相互调用，可以通过第三方转发该调用。其目的是降低类之间的耦合度，尽量减少对其他类的依赖，提高模块的相对独立性。<br>　　通俗的来讲，就是描述一个类对自己依赖的类知道的越少越好，无需关心是怎么实现的，对于被依赖的类来说，无论逻辑多么复杂，都尽量地的将逻辑封装在类的内部，对外除了提供的public方法，不对外泄漏任何信息。迪米特法则还有一个更简单的定义：只与直接的朋友通信。首先来解释一下什么是直接的朋友：迪米特法则中的“朋友”是指：当前对象本身、当前对象的成员对象、当前对象所创建的对象、当前对象的方法参数等，这些对象同当前对象存在关联、聚合或组合关系，可以直接访问这些对象的方法。只要两个对象之间有耦合关系，我们就说这两个对象之间是朋友关系。耦合的方式很多，依赖、关联、组合、聚合等。其中，我们称出现成员变量、方法参数、方法返回值中的类为直接的朋友，而出现在局部变量中的类则不是直接的朋友。也就是说，陌生的类最好不要作为局部变量的形式出现在类的内部。 所以一般应用迪米特法则设计的类都有一些特点，减少暴露类成员，类本身不能被改变，降低对类的访问权限等。</p>
<p>在《程序员修炼之道》这本书中有提到。</p>
<p>设计模式的门面模式（Facade）和中介者模式（Mediator），都是迪米特法则应用的例子。</p>
<p>迪米特法则优点：</p>
<p>1）降低了类之间的耦合度，提高了模块的相对独立性。</p>
<p>2）由于亲合度降低，从而提高了类的可复用性和系统的扩展性。</p>
<p>   迪米特法则缺点：</p>
<p>1）过分的使用迪米特原则，会产生大量这样的中间类和传递类，导致系统复杂度变大。</p>
<p> 使用原则：</p>
<p>使用迪米特原则时候要权衡好，保证系统架构的清晰。</p>
<h4 id="其他的设计原则"><a href="#其他的设计原则" class="headerlink" title="其他的设计原则"></a><strong>其他的设计原则</strong></h4><p>1.配置化：尽可能的使用配置，而不要使用硬编码的方式。比如像一些数据库参数及常量，应该放到配置文件中。</p>
<p>2.面向接口编程：只需要关心接口，不需要关心具体的实现。</p>
<p>3.DRY(Don’t Repeat Yourself )避免重复代码<strong>：</strong>抽取重复的代码封装成一个函数或者类方法，保持里面的逻辑一致，可以被其他使用者调用。</p>
<p> 4.<strong>组合优于继承（Com­po­si­tion over Inher­i­tance）:</strong> 这里有篇老外写的文章**<a href="http://theburningmonk.com/2015/03/this-is-why-you-need-composition-over-inheritance/">This is why you need Composition over Inheritance</a>。**</p>
<h2 id="（2）了解面向对象程序的设计和效率"><a href="#（2）了解面向对象程序的设计和效率" class="headerlink" title="（2）了解面向对象程序的设计和效率"></a>（2）了解面向对象程序的设计和效率</h2><p>面向对象编程基于三个基本概念：数据抽象封装，继承，动态绑定。</p>
<h3 id="面向对象程序设计的优点是什么？"><a href="#面向对象程序设计的优点是什么？" class="headerlink" title="面向对象程序设计的优点是什么？"></a>面向对象程序设计的优点是什么？</h3><p>1、易维护</p>
<p>采用面向对象思想设计的结构，可读性高，由于继承的存在，即使改变需求，那么维护也只是在局部模块，所以维护起来是非常方便和较低成本的。</p>
<p>2、质量高</p>
<p>在设计时，可重用现有的，在以前的项目的领域中已被测试过的类使系统满足业务需求并具有较高的质量。</p>
<p>3、效率高</p>
<p>在软件开发时，根据设计的需要对现实世界的事物进行抽象，产生类。使用这样的方法解决问题，接近于日常生活和自然的思考方式，势必提高软件开发的效率和质量。</p>
<p>4、易扩展</p>
<p>由于继承、封装、多态的特性，自然设计出高内聚、低耦合的系统结构，使得系统更灵活、更容易扩展，而且成本较低。</p>
<h3 id="面向对象方法效率与传统进行比较"><a href="#面向对象方法效率与传统进行比较" class="headerlink" title="面向对象方法效率与传统进行比较"></a>面向对象方法效率与传统进行比较</h3><p> <em>面向对象</em>开发模式更有利于人们开拓思维，在具体的开发过程中便于<em>程序</em>的划分，方便<em>程序</em>员分工合作，提高开发效率。</p>
<p>但是编译器运行时间相对增长。</p>
<h2 id="（4）掌握类的定义、对象的定义、对象的初始化"><a href="#（4）掌握类的定义、对象的定义、对象的初始化" class="headerlink" title="（4）掌握类的定义、对象的定义、对象的初始化"></a>（4）掌握类的定义、对象的定义、对象的初始化</h2><p>C++ 中的类（Class）可以看做C语言中结构体（Struct）的升级版。结构体是一种构造类型，可以包含若干成员变量，每个成员变量的类型可以不同；可以通过结构体来定义结构体变量，每个变量拥有相同的性质。例如：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">//定义结构体 Student</span></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Student</span>&#123;</span><br><span class="line">    <span class="comment">//结构体包含的成员变量</span></span><br><span class="line">    <span class="type">char</span> *name;</span><br><span class="line">    <span class="type">int</span> age;</span><br><span class="line">    <span class="type">float</span> score;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">//显示结构体的成员变量</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">display</span><span class="params">(<span class="keyword">struct</span> Student stu)</span></span>&#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;%s的年龄是 %d，成绩是 %f\n&quot;</span>, stu.name, stu.age, stu.score);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">Student</span> stu1;</span><br><span class="line">    <span class="comment">//为结构体的成员变量赋值</span></span><br><span class="line">    stu1.name = <span class="string">&quot;小明&quot;</span>;</span><br><span class="line">    stu1.age = <span class="number">15</span>;</span><br><span class="line">    stu1.score = <span class="number">92.5</span>;</span><br><span class="line">    <span class="comment">//调用函数</span></span><br><span class="line">    <span class="built_in">display</span>(stu1);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>运行结果：</p>
<p>小明的年龄是 15，成绩是 92.500000</p>
<p>C++ 中的类也是一种构造类型，但是进行了一些扩展，类的成员不但可以是变量，还可以是函数；通过类定义出来的变量也有特定的称呼，叫做“对象”。例如：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">//通过class关键字类定义类</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span>&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">//类包含的变量</span></span><br><span class="line">    <span class="type">char</span> *name;</span><br><span class="line">    <span class="type">int</span> age;</span><br><span class="line">    <span class="type">float</span> score;</span><br><span class="line">    <span class="comment">//类包含的函数</span></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">say</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%s的年龄是 %d，成绩是 %f\n&quot;</span>, name, age, score);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="comment">//通过类来定义变量，即创建对象</span></span><br><span class="line">    <span class="keyword">class</span> <span class="title class_">Student</span> stu1;  <span class="comment">//也可以省略关键字class</span></span><br><span class="line">    <span class="comment">//为类的成员变量赋值</span></span><br><span class="line">    stu1.name = <span class="string">&quot;小明&quot;</span>;</span><br><span class="line">    stu1.age = <span class="number">15</span>;</span><br><span class="line">    stu1.score = <span class="number">92.5f</span>;</span><br><span class="line">    <span class="comment">//调用类的成员函数</span></span><br><span class="line">    stu1.<span class="built_in">say</span>();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>运行结果与上例相同。</p>
<h3 id="类的定义"><a href="#类的定义" class="headerlink" title="类的定义"></a>类的定义</h3><p>类是用户自定义的类型，如果程序中要用到类，必须提前说明，或者使用已存在的类（别人写好的类、标准库中的类等），C++语法本身并不提供现成的类的名称、结构和内容。</p>
<p>一个简单的类的定义：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span>&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">//成员变量</span></span><br><span class="line">    <span class="type">char</span> *name;</span><br><span class="line">    <span class="type">int</span> age;</span><br><span class="line">    <span class="type">float</span> score;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//成员函数</span></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">say</span><span class="params">()</span></span>&#123;</span><br><span class="line">        cout&lt;&lt;name&lt;&lt;<span class="string">&quot;的年龄是&quot;</span>&lt;&lt;age&lt;&lt;<span class="string">&quot;，成绩是&quot;</span>&lt;&lt;score&lt;&lt;endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p><code>class</code>是 C++ 中新增的关键字，专门用来定义类。<code>Student</code>是类的名称；类名的首字母一般大写，以和其他的标识符区分开。<code>&#123; &#125;</code>内部是类所包含的成员变量和成员函数，它们统称为类的成员（Member）</p>
<blockquote>
<p>注意在类定义的最后有一个分号<code>;</code>，它是类定义的一部分，表示类定义结束了，不能省略。</p>
</blockquote>
<p>整体上讲，上面的代码创建了一个 Student 类，它包含了 3 个成员变量和 1 个成员函数。</p>
<p>类只是一个模板（Template），编译后不占用内存空间，所以在定义类时不能对成员变量进行初始化，因为没有地方存储数据。只有在创建对象以后才会给成员变量分配内存，这个时候就可以赋值了。</p>
<p>类可以理解为一种新的数据类型，该数据类型的名称是 Student。与 char、int、float 等基本数据类型不同的是，Student 是一种复杂数据类型，可以包含基本类型，而且还有很多基本类型中没有的特性，以后大家会见到。</p>
<h2 id="创建对象"><a href="#创建对象" class="headerlink" title="创建对象"></a>创建对象</h2><p>有了 Student 类后，就可以通过它来创建对象了，例如：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Student liLei;  <span class="comment">//创建对象</span></span><br></pre></td></tr></table></figure>

<p><code>Student</code>是类名，<code>liLei</code>是对象名。这和使用基本类型定义变量的形式类似：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> a;  <span class="comment">//定义整型变量</span></span><br></pre></td></tr></table></figure>

<p>从这个角度考虑，我们可以把 Student 看做一种新的数据类型，把 liLei 看做一个变量。</p>
<p>在创建对象时，class 关键字可要可不要，但是出于习惯我们通常会省略掉 class 关键字，例如：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span> LiLei;  <span class="comment">//正确Student LiLei;  //同样正确</span></span><br></pre></td></tr></table></figure>

<p>除了创建单个对象，还可以创建对象数组：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Student allStu[<span class="number">100</span>];</span><br></pre></td></tr></table></figure>

<p>该语句创建了一个 allStu 数组，它拥有100个元素，每个元素都是 Student 类型的对象。</p>
<h2 id="访问类的成员"><a href="#访问类的成员" class="headerlink" title="访问类的成员"></a>访问类的成员</h2><p>创建对象以后，可以使用点号<code>.</code>来访问成员变量和成员函数，这和通过结构体变量来访问它的成员类似，如下所示：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="comment">//类通常定义在函数外面</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span>&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">//类包含的变量</span></span><br><span class="line">    <span class="type">char</span> *name;</span><br><span class="line">    <span class="type">int</span> age;</span><br><span class="line">    <span class="type">float</span> score;</span><br><span class="line">    <span class="comment">//类包含的函数</span></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">say</span><span class="params">()</span></span>&#123;</span><br><span class="line">        cout&lt;&lt;name&lt;&lt;<span class="string">&quot;的年龄是&quot;</span>&lt;&lt;age&lt;&lt;<span class="string">&quot;，成绩是&quot;</span>&lt;&lt;score&lt;&lt;endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="comment">//创建对象</span></span><br><span class="line">    Student stu;</span><br><span class="line">    stu.name = <span class="string">&quot;小明&quot;</span>;</span><br><span class="line">    stu.age = <span class="number">15</span>;</span><br><span class="line">    stu.score = <span class="number">92.5f</span>;</span><br><span class="line">    stu.<span class="built_in">say</span>();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>运行结果：<br>小明的年龄是15，成绩是92.5</p>
<p>stu 是一个对象，占用内存空间，可以对它的成员变量赋值，也可以读取它的成员变量。</p>
<p>类通常定义在函数外面，当然也可以定义在函数内部，不过很少这样使用。</p>
<h2 id="使用对象指针"><a href="#使用对象指针" class="headerlink" title="使用对象指针"></a>使用对象指针</h2><p>C语言中经典的指针在 C++ 中仍然广泛使用，尤其是指向对象的指针，没有它就不能实现某些功能。</p>
<p>上面代码中创建的对象 stu 在栈上分配内存，需要使用<code>&amp;</code>获取它的地址，例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Student stu;Student *pStu = &amp;stu;</span><br></pre></td></tr></table></figure>

<p>pStu 是一个指针，它指向 Student 类型的数据，也就是通过 Student 创建出来的对象。</p>
<p>当然，你也可以在堆上创建对象，这个时候就需要使用前面讲到的<code>new</code>关键字（<a href="http://c.biancheng.net/view/2199.html">C++ new和delete运算符简介</a>），例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Student *pStu = new Student;</span><br></pre></td></tr></table></figure>

<p>在栈上创建出来的对象都有一个名字，比如 stu，使用指针指向它不是必须的。但是通过 new 创建出来的对象就不一样了，它在堆上分配内存，没有名字，只能得到一个指向它的指针，所以必须使用一个指针变量来接收这个指针，否则以后再也无法找到这个对象了，更没有办法使用它。也就是说，使用 new 在堆上创建出来的对象是匿名的，没法直接使用，必须要用一个指针指向它，再借助指针来访问它的成员变量或成员函数。</p>
<p>栈内存是程序自动管理的，不能使用 delete 删除在栈上创建的对象；堆内存由程序员管理，对象使用完毕后可以通过 delete 删除。在实际开发中，new 和 delete 往往成对出现，以保证及时删除不再使用的对象，防止无用内存堆积。</p>
<blockquote>
<p>栈（Stack）和堆（Heap）是 C&#x2F;C++ 程序员必须要了解的两个概念，在《<a href="http://c.biancheng.net/c/140/">C语言内存精讲</a>》专题中进行了深入讲解</p>
</blockquote>
<p>有了对象指针后，可以通过箭头<code>-&gt;</code>来访问对象的成员变量和成员函数，这和通过<a href="http://c.biancheng.net/view/246.html">结构体指针</a>来访问它的成员类似，请看下面的示例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">pStu -&gt; name = &quot;小明&quot;;pStu -&gt; age = 15;pStu -&gt; score = 92.5f;pStu -&gt; say();</span><br></pre></td></tr></table></figure>

<p>下面是一个完整的例子：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span>&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="type">char</span> *name;</span><br><span class="line">    <span class="type">int</span> age;</span><br><span class="line">    <span class="type">float</span> score;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">say</span><span class="params">()</span></span>&#123;</span><br><span class="line">        cout&lt;&lt;name&lt;&lt;<span class="string">&quot;的年龄是&quot;</span>&lt;&lt;age&lt;&lt;<span class="string">&quot;，成绩是&quot;</span>&lt;&lt;score&lt;&lt;endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    Student *pStu = <span class="keyword">new</span> Student;</span><br><span class="line">    pStu -&gt; name = <span class="string">&quot;小明&quot;</span>;</span><br><span class="line">    pStu -&gt; age = <span class="number">15</span>;</span><br><span class="line">    pStu -&gt; score = <span class="number">92.5f</span>;</span><br><span class="line">    pStu -&gt; <span class="built_in">say</span>();</span><br><span class="line">    <span class="keyword">delete</span> pStu;  <span class="comment">//删除对象</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>运行结果：<br>小明的年龄是15，成绩是92.5</p>
<p>虽然在一般的程序中无视垃圾内存影响不大，但记得 delete 掉不再使用的对象依然是一种良好的编程习惯。</p>
<h2 id="（5）掌握成员函数的定义及使用"><a href="#（5）掌握成员函数的定义及使用" class="headerlink" title="（5）掌握成员函数的定义及使用"></a>（5）掌握成员函数的定义及使用</h2><p>类可以看做是一种数据类型，它类似于普通的数据类型，但是又有别于普通的数据类型。类这种数据类型是一个包含成员变量和成员函数的集合。</p>
<p>类的成员变量和普通变量一样，也有数据类型和名称，占用固定长度的内存。但是，在定义类的时候不能对成员变量赋值，因为类只是一种数据类型或者说是一种模板，本身不占用内存空间，而变量的值则需要内存来存储。</p>
<p>类的成员函数也和普通函数一样，都有返回值和参数列表，它与一般函数的区别是：成员函数是一个类的成员，出现在类体中，它的作用范围由类来决定；而普通函数是独立的，作用范围是全局的，或位于某个命名空间内。</p>
<p>Student 类的定义，如下所示：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span>&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">//成员变量</span></span><br><span class="line">    <span class="type">char</span> *name;</span><br><span class="line">    <span class="type">int</span> age;</span><br><span class="line">    <span class="type">float</span> score;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//成员函数</span></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">say</span><span class="params">()</span></span>&#123;</span><br><span class="line">        cout&lt;&lt;name&lt;&lt;<span class="string">&quot;的年龄是&quot;</span>&lt;&lt;age&lt;&lt;<span class="string">&quot;，成绩是&quot;</span>&lt;&lt;score&lt;&lt;endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>这段代码在类体中定义了成员函数。你也可以只在类体中声明函数，而将函数定义放在类体外面，如下图所示：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span>&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">//成员变量</span></span><br><span class="line">    <span class="type">char</span> *name;</span><br><span class="line">    <span class="type">int</span> age;</span><br><span class="line">    <span class="type">float</span> score;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//成员函数</span></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">say</span><span class="params">()</span></span>;  <span class="comment">//函数声明</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">//函数定义</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">Student::say</span><span class="params">()</span></span>&#123;</span><br><span class="line">    cout&lt;&lt;name&lt;&lt;<span class="string">&quot;的年龄是&quot;</span>&lt;&lt;age&lt;&lt;<span class="string">&quot;，成绩是&quot;</span>&lt;&lt;score&lt;&lt;endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在类体中直接定义函数时，不需要在函数名前面加上类名，因为函数属于哪一个类是不言而喻的。</p>
<p>当成员函数定义在类外时，就必须在函数名前面加上类名予以限定。<code>::</code>被称为域解析符（也称作用域运算符或作用域限定符），用来连接类名和函数名，指明当前函数属于哪个类。</p>
<p>成员函数必须先在类体中作原型声明，然后在类外定义，也就是说类体的位置应在函数定义之前。</p>
<h3 id="在类体中和类体外定义成员函数的区别"><a href="#在类体中和类体外定义成员函数的区别" class="headerlink" title="在类体中和类体外定义成员函数的区别"></a>在类体中和类体外定义成员函数的区别</h3><p>在类体中和类体外定义成员函数是有区别的：在类体中定义的成员函数会自动成为内联函数，在类体外定义的不会。当然，在类体内部定义的函数也可以加 inline 关键字，但这是多余的，因为类体内部定义的函数默认就是内联函数。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span>&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="type">char</span> *name;</span><br><span class="line">    <span class="type">int</span> age;</span><br><span class="line">    <span class="type">float</span> score;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">say</span><span class="params">()</span></span>;  <span class="comment">//内联函数声明，可以增加 inline 关键字，但编译器会忽略</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">//函数定义</span></span><br><span class="line"><span class="function"><span class="keyword">inline</span> <span class="type">void</span> <span class="title">Student::say</span><span class="params">()</span></span>&#123;</span><br><span class="line">    cout&lt;&lt;name&lt;&lt;<span class="string">&quot;的年龄是&quot;</span>&lt;&lt;age&lt;&lt;<span class="string">&quot;，成绩是&quot;</span>&lt;&lt;score&lt;&lt;endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>引入内联函数的目的是为了解决程序中函数调用的效率问题，这么说吧，程序在编译器编译的时候，编译器将程序中出现的内联函数的调用表达式用内联函数的函数体进行替换，而对于其他的函数，都是在运行时候才被替代。这其实就是个空间代价换时间的i节省。所以内联函数一般都是1-5行的小函数。在使用内联函数时要留神：</p>
<ul>
<li>1.在内联函数内不允许使用循环语句和开关语句；</li>
<li>2.内联函数的定义必须出现在内联函数第一次调用之前；</li>
<li>3.类结构中所在的类说明内部定义的函数是内联函数。</li>
</ul>
<h3 id="类成员的访问权限以及类的封装"><a href="#类成员的访问权限以及类的封装" class="headerlink" title="类成员的访问权限以及类的封装"></a>类成员的访问权限以及类的封装</h3><p><a href="http://c.biancheng.net/cplus/">C++</a>通过 public、protected、private 三个关键字来控制成员变量和成员函数的访问权限，它们分别表示公有的、受保护的、私有的，被称为成员访问限定符。</p>
<p>在类的内部（定义类的代码内部），无论成员被声明为 public、protected 还是 private，都是可以互相访问的，没有访问权限的限制。</p>
<p>在类的外部（定义类的代码之外），只能通过对象访问成员，并且通过对象只能访问 public 属性的成员，不能访问 private、protected 属性的成员。</p>
<p>下面通过一个 Student 类来演示成员的访问权限：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="comment">//类的声明</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span>&#123;</span><br><span class="line"><span class="keyword">private</span>:  <span class="comment">//私有的</span></span><br><span class="line">    <span class="type">char</span> *m_name;</span><br><span class="line">    <span class="type">int</span> m_age;</span><br><span class="line">    <span class="type">float</span> m_score;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:  <span class="comment">//共有的</span></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">setname</span><span class="params">(<span class="type">char</span> *name)</span></span>;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">setage</span><span class="params">(<span class="type">int</span> age)</span></span>;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">setscore</span><span class="params">(<span class="type">float</span> score)</span></span>;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">show</span><span class="params">()</span></span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">//成员函数的定义</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">Student::setname</span><span class="params">(<span class="type">char</span> *name)</span></span>&#123;</span><br><span class="line">    m_name = name;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">Student::setage</span><span class="params">(<span class="type">int</span> age)</span></span>&#123;</span><br><span class="line">    m_age = age;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">Student::setscore</span><span class="params">(<span class="type">float</span> score)</span></span>&#123;</span><br><span class="line">    m_score = score;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">Student::show</span><span class="params">()</span></span>&#123;</span><br><span class="line">    cout&lt;&lt;m_name&lt;&lt;<span class="string">&quot;的年龄是&quot;</span>&lt;&lt;m_age&lt;&lt;<span class="string">&quot;，成绩是&quot;</span>&lt;&lt;m_score&lt;&lt;endl;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="comment">//在栈上创建对象</span></span><br><span class="line">    Student stu;</span><br><span class="line">    stu.<span class="built_in">setname</span>(<span class="string">&quot;小明&quot;</span>);</span><br><span class="line">    stu.<span class="built_in">setage</span>(<span class="number">15</span>);</span><br><span class="line">    stu.<span class="built_in">setscore</span>(<span class="number">92.5f</span>);</span><br><span class="line">    stu.<span class="built_in">show</span>();</span><br><span class="line"></span><br><span class="line">    <span class="comment">//在堆上创建对象</span></span><br><span class="line">    Student *pstu = <span class="keyword">new</span> Student;</span><br><span class="line">    pstu -&gt; <span class="built_in">setname</span>(<span class="string">&quot;李华&quot;</span>);</span><br><span class="line">    pstu -&gt; <span class="built_in">setage</span>(<span class="number">16</span>);</span><br><span class="line">    pstu -&gt; <span class="built_in">setscore</span>(<span class="number">96</span>);</span><br><span class="line">    pstu -&gt; <span class="built_in">show</span>();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>运行结果：<br>小明的年龄是15，成绩是92.5<br>李华的年龄是16，成绩是96</p>
<p>类的声明和成员函数的定义都是类定义的一部分，在实际开发中，我们通常将类的声明放在头文件中，而将成员函数的定义放在源文件中。</p>
<p>类中的成员变量 m_name、m_age 和m_ score 被设置成 private 属性，在类的外部不能通过对象访问。也就是说，私有成员变量和成员函数只能在类内部使用，在类外都是无效的。</p>
<p>成员函数 setname()、setage() 和 setscore() 被设置为 public 属性，是公有的，可以通过对象访问。</p>
<p>private 后面的成员都是私有的，直到有 public 出现才会变成共有的；public 之后再无其他限定符，所以 public 后面的成员都是共有的。</p>
<p>成员变量大都以<code>m_</code>开头，这是约定成俗的写法，不是语法规定的内容。以<code>m_</code>开头既可以一眼看出这是成员变量，又可以和成员函数中的形参名字区分开。</p>
<h2 id="（6）掌握构造函数与析构函数的功能、使用方法与调用顺序"><a href="#（6）掌握构造函数与析构函数的功能、使用方法与调用顺序" class="headerlink" title="（6）掌握构造函数与析构函数的功能、使用方法与调用顺序"></a>（6）掌握构造函数与析构函数的功能、使用方法与调用顺序</h2><h3 id="构造函数"><a href="#构造函数" class="headerlink" title="构造函数"></a>构造函数</h3><p>有一种特殊的成员函数，它的名字和类名相同，没有返回值，不需要用户显式调用（用户也不能调用），而是在创建对象时自动执行。这种特殊的成员函数就是构造函数（Constructor）。</p>
<p>之前，我们通过成员函数 setname()、setage()、setscore() 分别为成员变量 name、age、score 赋值，这样做虽然有效，但显得有点麻烦。有了构造函数，我们就可以简化这项工作，在创建对象的同时为成员变量赋值，请看下面的代码（示例1）：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span>&#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">char</span> *m_name;</span><br><span class="line">    <span class="type">int</span> m_age;</span><br><span class="line">    <span class="type">float</span> m_score;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">//声明构造函数</span></span><br><span class="line">    <span class="built_in">Student</span>(<span class="type">char</span> *name, <span class="type">int</span> age, <span class="type">float</span> score);</span><br><span class="line">    <span class="comment">//声明普通成员函数</span></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">show</span><span class="params">()</span></span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">//定义构造函数</span></span><br><span class="line">Student::<span class="built_in">Student</span>(<span class="type">char</span> *name, <span class="type">int</span> age, <span class="type">float</span> score)&#123;</span><br><span class="line">    m_name = name;</span><br><span class="line">    m_age = age;</span><br><span class="line">    m_score = score;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//定义普通成员函数</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">Student::show</span><span class="params">()</span></span>&#123;</span><br><span class="line">    cout&lt;&lt;m_name&lt;&lt;<span class="string">&quot;的年龄是&quot;</span>&lt;&lt;m_age&lt;&lt;<span class="string">&quot;，成绩是&quot;</span>&lt;&lt;m_score&lt;&lt;endl;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="comment">//创建对象时向构造函数传参</span></span><br><span class="line">    <span class="function">Student <span class="title">stu</span><span class="params">(<span class="string">&quot;小明&quot;</span>, <span class="number">15</span>, <span class="number">92.5f</span>)</span></span>;</span><br><span class="line">    stu.<span class="built_in">show</span>();</span><br><span class="line">    <span class="comment">//创建对象时向构造函数传参</span></span><br><span class="line">    Student *pstu = <span class="keyword">new</span> <span class="built_in">Student</span>(<span class="string">&quot;李华&quot;</span>, <span class="number">16</span>, <span class="number">96</span>);</span><br><span class="line">    pstu -&gt; <span class="built_in">show</span>();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>运行结果：<br>小明的年龄是15，成绩是92.5<br>李华的年龄是16，成绩是96</p>
<p>该例在 Student 类中定义了一个构造函数<code>Student(char *, int, float)</code>，它的作用是给三个 private 属性的成员变量赋值。要想调用该构造函数，就得在创建对象的同时传递实参，并且实参由<code>( )</code>包围，和普通的函数调用非常类似。</p>
<p>在栈上创建对象时，实参位于对象名后面，例如<code>Student stu(&quot;小明&quot;, 15, 92.5f)</code>；在堆上创建对象时，实参位于类名后面，例如<code>new Student(&quot;李华&quot;, 16, 96)</code>。</p>
<p>构造函数必须是 public 属性的，否则创建对象时无法调用。当然，设置为 private、protected 属性也不会报错，但是没有意义。</p>
<p>构造函数没有返回值，因为没有变量来接收返回值，即使有也毫无用处，这意味着：</p>
<ul>
<li>不管是声明还是定义，函数名前面都不能出现返回值类型，即使是 void 也不允许；</li>
<li>函数体中不能有 return 语句。</li>
</ul>
<h4 id="构造函数的重载"><a href="#构造函数的重载" class="headerlink" title="构造函数的重载"></a>构造函数的重载</h4><p>和普通成员函数一样，构造函数是允许重载的。一个类可以有多个重载的构造函数，创建对象时根据传递的实参来判断调用哪一个构造函数。</p>
<p>构造函数的调用是强制性的，一旦在类中定义了构造函数，那么创建对象时就一定要调用，不调用是错误的。如果有多个重载的构造函数，那么创建对象时提供的实参必须和其中的一个构造函数匹配；反过来说，创建对象时只有一个构造函数会被调用。</p>
<p>对示例1中的代码，如果写作<code>Student stu</code>或者<code>new Student</code>就是错误的，因为类中包含了构造函数，而创建对象时却没有调用。</p>
<p>更改示例1的代码，再添加一个构造函数（示例2）：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span>&#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">char</span> *m_name;</span><br><span class="line">    <span class="type">int</span> m_age;</span><br><span class="line">    <span class="type">float</span> m_score;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Student</span>();</span><br><span class="line">    <span class="built_in">Student</span>(<span class="type">char</span> *name, <span class="type">int</span> age, <span class="type">float</span> score);</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">setname</span><span class="params">(<span class="type">char</span> *name)</span></span>;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">setage</span><span class="params">(<span class="type">int</span> age)</span></span>;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">setscore</span><span class="params">(<span class="type">float</span> score)</span></span>;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">show</span><span class="params">()</span></span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">Student::<span class="built_in">Student</span>()&#123;</span><br><span class="line">    m_name = <span class="literal">NULL</span>;</span><br><span class="line">    m_age = <span class="number">0</span>;</span><br><span class="line">    m_score = <span class="number">0.0</span>;</span><br><span class="line">&#125;</span><br><span class="line">Student::<span class="built_in">Student</span>(<span class="type">char</span> *name, <span class="type">int</span> age, <span class="type">float</span> score)&#123;</span><br><span class="line">    m_name = name;</span><br><span class="line">    m_age = age;</span><br><span class="line">    m_score = score;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">Student::setname</span><span class="params">(<span class="type">char</span> *name)</span></span>&#123;</span><br><span class="line">    m_name = name;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">Student::setage</span><span class="params">(<span class="type">int</span> age)</span></span>&#123;</span><br><span class="line">    m_age = age;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">Student::setscore</span><span class="params">(<span class="type">float</span> score)</span></span>&#123;</span><br><span class="line">    m_score = score;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">Student::show</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(m_name == <span class="literal">NULL</span> || m_age &lt;= <span class="number">0</span>)&#123;</span><br><span class="line">        cout&lt;&lt;<span class="string">&quot;成员变量还未初始化&quot;</span>&lt;&lt;endl;</span><br><span class="line">    &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">        cout&lt;&lt;m_name&lt;&lt;<span class="string">&quot;的年龄是&quot;</span>&lt;&lt;m_age&lt;&lt;<span class="string">&quot;，成绩是&quot;</span>&lt;&lt;m_score&lt;&lt;endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="comment">//调用构造函数 Student(char *, int, float)</span></span><br><span class="line">    <span class="function">Student <span class="title">stu</span><span class="params">(<span class="string">&quot;小明&quot;</span>, <span class="number">15</span>, <span class="number">92.5f</span>)</span></span>;</span><br><span class="line">    stu.<span class="built_in">show</span>();</span><br><span class="line"></span><br><span class="line">    <span class="comment">//调用构造函数 Student()</span></span><br><span class="line">    Student *pstu = <span class="keyword">new</span> <span class="built_in">Student</span>();</span><br><span class="line">    pstu -&gt; <span class="built_in">show</span>();</span><br><span class="line">    pstu -&gt; <span class="built_in">setname</span>(<span class="string">&quot;李华&quot;</span>);</span><br><span class="line">    pstu -&gt; <span class="built_in">setage</span>(<span class="number">16</span>);</span><br><span class="line">    pstu -&gt; <span class="built_in">setscore</span>(<span class="number">96</span>);</span><br><span class="line">    pstu -&gt; <span class="built_in">show</span>();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>运行结果：<br>小明的年龄是15，成绩是92.5<br>成员变量还未初始化<br>李华的年龄是16，成绩是96</p>
<p>构造函数<code>Student(char *, int, float)</code>为各个成员变量赋值，构造函数<code>Student()</code>将各个成员变量的值设置为空，它们是重载关系。根据<code>Student()</code>创建对象时不会赋予成员变量有效值，所以还要调用成员函数 setname()、setage()、setscore() 来给它们重新赋值。</p>
<p>构造函数在实际开发中会大量使用，它往往用来做一些初始化工作，例如对成员变量赋值、预先打开文件等。</p>
<h4 id="默认构造函数"><a href="#默认构造函数" class="headerlink" title="默认构造函数"></a>默认构造函数</h4><p>如果用户自己没有定义构造函数，那么编译器会自动生成一个默认的构造函数，只是这个构造函数的函数体是空的，也没有形参，也不执行任何操作。比如上面的 Student 类，默认生成的构造函数如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Student()&#123;&#125;</span><br></pre></td></tr></table></figure>

<p>一个类必须有构造函数，要么用户自己定义，要么编译器自动生成。一旦用户自己定义了构造函数，不管有几个，也不管形参如何，编译器都不再自动生成。在示例1中，Student 类已经有了一个构造函数<code>Student(char *, int, float)</code>，也就是我们自己定义的，编译器不会再额外添加构造函数<code>Student()</code>，在示例2中我们才手动添加了该构造函数。</p>
<blockquote>
<p>实际上编译器只有在必要的时候才会生成默认构造函数，而且它的函数体一般不为空。默认构造函数的目的是帮助编译器做初始化工作，而不是帮助程序员。这是C++的内部实现机制，这里不再深究，初学者可以按照上面说的“一定有一个空函数体的默认构造函数”来理解。</p>
</blockquote>
<p>最后需要注意的一点是，调用没有参数的构造函数也可以省略括号。对于示例2的代码，在栈上创建对象可以写作<code>Student stu()</code>或<code>Student stu</code>，在堆上创建对象可以写作<code>Student *pstu = new Student()</code>或<code>Student *pstu = new Student</code>，它们都会调用构造函数 Student()。</p>
<h4 id="构造函数初始化列表"><a href="#构造函数初始化列表" class="headerlink" title="构造函数初始化列表"></a>构造函数初始化列表</h4><p>构造函数的一项重要功能是对成员变量进行初始化，为了达到这个目的，可以在构造函数的函数体中对成员变量一一赋值，还可以采用初始化列表。</p>
<p>构造函数的初始化列表使得代码更加简洁，请看下面的例子：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span>&#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">char</span> *m_name;</span><br><span class="line">    <span class="type">int</span> m_age;</span><br><span class="line">    <span class="type">float</span> m_score;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Student</span>(<span class="type">char</span> *name, <span class="type">int</span> age, <span class="type">float</span> score);</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">show</span><span class="params">()</span></span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">//采用初始化列表</span></span><br><span class="line">Student::<span class="built_in">Student</span>(<span class="type">char</span> *name, <span class="type">int</span> age, <span class="type">float</span> score): <span class="built_in">m_name</span>(name), <span class="built_in">m_age</span>(age), <span class="built_in">m_score</span>(score)&#123;</span><br><span class="line">    <span class="comment">//<span class="doctag">TODO:</span></span></span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">Student::show</span><span class="params">()</span></span>&#123;</span><br><span class="line">    cout&lt;&lt;m_name&lt;&lt;<span class="string">&quot;的年龄是&quot;</span>&lt;&lt;m_age&lt;&lt;<span class="string">&quot;，成绩是&quot;</span>&lt;&lt;m_score&lt;&lt;endl;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="function">Student <span class="title">stu</span><span class="params">(<span class="string">&quot;小明&quot;</span>, <span class="number">15</span>, <span class="number">92.5f</span>)</span></span>;</span><br><span class="line">    stu.<span class="built_in">show</span>();</span><br><span class="line">    Student *pstu = <span class="keyword">new</span> <span class="built_in">Student</span>(<span class="string">&quot;李华&quot;</span>, <span class="number">16</span>, <span class="number">96</span>);</span><br><span class="line">    pstu -&gt; <span class="built_in">show</span>();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>运行结果：<br>小明的年龄是15，成绩是92.5<br>李华的年龄是16，成绩是96</p>
<p>如本例所示，定义构造函数时并没有在函数体中对成员变量一一赋值，其函数体为空（当然也可以有其他语句），而是在函数首部与函数体之间添加了一个冒号<code>:</code>，后面紧跟<code>m_name(name), m_age(age), m_score(score)</code>语句，这个语句的意思相当于函数体内部的<code>m_name = name; m_age = age; m_score = score;</code>语句，也是赋值的意思。</p>
<p>使用构造函数初始化列表并没有效率上的优势，仅仅是书写方便，尤其是成员变量较多时，这种写法非常简单明了。</p>
<p>初始化列表可以用于全部成员变量，也可以只用于部分成员变量。下面的示例只对 m_name 使用初始化列表，其他成员变量还是一一赋值：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Student::<span class="built_in">Student</span>(<span class="type">char</span> *name, <span class="type">int</span> age, <span class="type">float</span> score): <span class="built_in">m_name</span>(name)&#123;</span><br><span class="line">    m_age = age;</span><br><span class="line">    m_score = score;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>注意，成员变量的初始化顺序与初始化列表中列出的变量的顺序无关，它只与成员变量在类中声明的顺序有关。请看代码：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Demo</span>&#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">int</span> m_a;</span><br><span class="line">    <span class="type">int</span> m_b;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Demo</span>(<span class="type">int</span> b);</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">show</span><span class="params">()</span></span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">Demo::<span class="built_in">Demo</span>(<span class="type">int</span> b): <span class="built_in">m_b</span>(b), <span class="built_in">m_a</span>(m_b)&#123; &#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">Demo::show</span><span class="params">()</span></span>&#123; cout&lt;&lt;m_a&lt;&lt;<span class="string">&quot;, &quot;</span>&lt;&lt;m_b&lt;&lt;endl; &#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="function">Demo <span class="title">obj</span><span class="params">(<span class="number">100</span>)</span></span>;</span><br><span class="line">    obj.<span class="built_in">show</span>();</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>运行结果：<br>2130567168, 100</p>
<p>在初始化列表中，我们将 m_b 放在了 m_a 的前面，看起来是先给 m_b 赋值，再给 m_a 赋值，其实不然！成员变量的赋值顺序由它们在类中的声明顺序决定，在 Demo 类中，我们先声明的 m_a，再声明的 m_b，所以构造函数和下面的代码等价：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Demo::<span class="built_in">Demo</span>(<span class="type">int</span> b): <span class="built_in">m_b</span>(b), <span class="built_in">m_a</span>(m_b)&#123;</span><br><span class="line">    m_a = m_b;</span><br><span class="line">    m_b = b;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>给 m_a 赋值时，m_b 还未被初始化，它的值是不确定的，所以输出的 m_a 的值是一个奇怪的数字；给 m_a 赋值完成后才给 m_b 赋值，此时 m_b 的值才是 100。</p>
<blockquote>
<p>obj 在栈上分配内存，成员变量的初始值是不确定的。</p>
</blockquote>
<h4 id="初始化-const-成员变量"><a href="#初始化-const-成员变量" class="headerlink" title="初始化 const 成员变量"></a>初始化 const 成员变量</h4><p>构造函数初始化列表还有一个很重要的作用，那就是初始化 const 成员变量。初始化 const 成员变量的唯一方法就是使用初始化列表。例如 VS&#x2F;VC 不支持变长数组（数组长度不能是变量），我们自己定义了一个 VLA 类，用于模拟变长数组，请看下面的代码：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">VLA</span>&#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">const</span> <span class="type">int</span> m_len;</span><br><span class="line">    <span class="type">int</span> *m_arr;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">VLA</span>(<span class="type">int</span> len);</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">//必须使用初始化列表来初始化 m_len</span></span><br><span class="line">VLA::<span class="built_in">VLA</span>(<span class="type">int</span> len): <span class="built_in">m_len</span>(len)&#123;</span><br><span class="line">    m_arr = <span class="keyword">new</span> <span class="type">int</span>[len];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>VLA 类包含了两个成员变量，m_len 和 m_arr <a href="http://c.biancheng.net/c/80/">指针</a>，需要注意的是 m_len 加了 const 修饰，只能使用初始化列表的方式赋值，如果写作下面的形式是错误的：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">VLA</span>&#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">const</span> <span class="type">int</span> m_len;</span><br><span class="line">    <span class="type">int</span> *m_arr;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">VLA</span>(<span class="type">int</span> len);</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">VLA::<span class="built_in">VLA</span>(<span class="type">int</span> len)&#123;</span><br><span class="line">    m_len = len;</span><br><span class="line">    m_arr = <span class="keyword">new</span> <span class="type">int</span>[len];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="析构函数"><a href="#析构函数" class="headerlink" title="析构函数"></a>析构函数</h3><p>创建对象时系统会自动调用构造函数进行初始化工作，同样，销毁对象时系统也会自动调用一个函数来进行清理工作，例如释放分配的内存、关闭打开的文件等，这个函数就是析构函数。</p>
<p>##（7）了解静态成员的必要性</p>
<h2 id="（8）了解静态成员、静态数据成员、成员函数的定义及使用"><a href="#（8）了解静态成员、静态数据成员、成员函数的定义及使用" class="headerlink" title="（8）了解静态成员、静态数据成员、成员函数的定义及使用"></a>（8）了解静态成员、静态数据成员、成员函数的定义及使用</h2><h2 id="（9）了解需要友元的原因、友元函数的使用"><a href="#（9）了解需要友元的原因、友元函数的使用" class="headerlink" title="（9）了解需要友元的原因、友元函数的使用"></a>（9）了解需要友元的原因、友元函数的使用</h2><p>友元是一种<strong>定义在类外部的普通函数</strong>，但它需要<strong>在类体内进行声明</strong>，为了与该类的成员函数加以区别，在声明时前面加以关键字friend。<strong>友元不是成员函数，但是它可以访问类中的私有成员</strong>。友元的作用在于提高程序的运行效率，但是，<strong>它破坏了类的封装性和隐藏性</strong>，使得非成员函数可以访问类的私有成员。</p>
<h2 id="（10）掌握继承、多态与重载的概念"><a href="#（10）掌握继承、多态与重载的概念" class="headerlink" title="（10）掌握继承、多态与重载的概念"></a>（10）掌握继承、多态与重载的概念</h2><h2 id="（11）掌握继承的工作方式、派生类的构造"><a href="#（11）掌握继承的工作方式、派生类的构造" class="headerlink" title="（11）掌握继承的工作方式、派生类的构造"></a>（11）掌握继承的工作方式、派生类的构造</h2><h2 id="（12）掌握虚函数的定义及使用"><a href="#（12）掌握虚函数的定义及使用" class="headerlink" title="（12）掌握虚函数的定义及使用"></a>（12）掌握虚函数的定义及使用</h2><h2 id="（13）掌握多继承的构造顺序、继承的访问控制"><a href="#（13）掌握多继承的构造顺序、继承的访问控制" class="headerlink" title="（13）掌握多继承的构造顺序、继承的访问控制"></a>（13）掌握多继承的构造顺序、继承的访问控制</h2><h2 id="（14）掌握运算符重载的使用"><a href="#（14）掌握运算符重载的使用" class="headerlink" title="（14）掌握运算符重载的使用"></a>（14）掌握运算符重载的使用</h2><h2 id="（15）了解抽象类的定义与使用"><a href="#（15）了解抽象类的定义与使用" class="headerlink" title="（15）了解抽象类的定义与使用"></a>（15）了解抽象类的定义与使用</h2>]]></content>
      <categories>
        <category>coding</category>
      </categories>
      <tags>
        <tag>编程</tag>
      </tags>
  </entry>
  <entry>
    <title>西瓜书第一章</title>
    <url>/posts/65b5ea6b/</url>
    <content><![CDATA[<h1 id="绪论"><a href="#绪论" class="headerlink" title="绪论"></a>绪论</h1><h2 id="定义"><a href="#定义" class="headerlink" title="定义"></a>定义</h2><p>假设用 P 来评估计算机程序在某任务类 T上的性能， 若一个程序通过利用经验E在T中任务丰获得了性 能改善，则我们就说关于T和P， 该程序对E进行了学习。</p>
<h2 id="基本术语"><a href="#基本术语" class="headerlink" title="基本术语"></a>基本术语</h2><h3 id="监督学习"><a href="#监督学习" class="headerlink" title="监督学习"></a>监督学习</h3><p>有时整个数据集亦称一 个”样本”因为它可看作对样本空间的一个采样， 通过上下文可判断出”样 本”是指单个示例还是数据集。</p>
<p>若我们欲预测的是离散值，此类学习任务称为”回归” (regression)。只涉及两个类别的”二分类” (binary clasification)任务，通常称其中一个类为 “正类” (positive class) 另一个类为”反类” (negative class); 涉及多个类别时，则称为”多分类” (multi-class classification)任务。</p>
<h3 id="无监督学习"><a href="#无监督学习" class="headerlink" title="无监督学习"></a>无监督学习</h3><p>不知道数学分类的时候，通过数据之间的关系分成”簇” (cluster)，称之为聚类” (clustering)。</p>
<h3 id="几个原则"><a href="#几个原则" class="headerlink" title="几个原则"></a>几个原则</h3><p>$\sum_f E_{\text {ote }}\left(\mathfrak{L}<em>a \mid X, f\right)&#x3D;\sum_f E</em>{\text {ote }}\left(\mathfrak{L}_b \mid X, f\right)$</p>
<p>无论学习算法$\mathfrak{L}_a$多聪明、学习算法$\mathfrak{L}_b$多笨拙，它们的期望性能相同!这就是”没有免费的午餐”定理 (No Free Lunch Theorem，简称 NFL定理) [Wolpert, 1996; Wolpert and Macready, 1995].</p>
<p>奥卡姆剃刀原则主张选择与经验观察一致的最简单假设 .</p>
]]></content>
      <categories>
        <category>machine learning</category>
      </categories>
      <tags>
        <tag>machine learning</tag>
      </tags>
  </entry>
</search>
